{
  "_what": "The overnight fleet. One agent per feature area, each with owned files, the gates that guard it, and the named defects it is accountable for.",
  "_why_verification_not_autofix": "Every agent here VERIFIES and REPORTS. None of them edits code unattended. That is deliberate: every serious outage on this project came from an unattended change that passed static checks — a blank homepage, a white /auth, every crawled row discarded. An agent that reports a defect with evidence costs nothing if it is wrong. An agent that edits at 4am and is wrong costs a morning. Fixes are approved in the morning and applied inside a cell.",
  "agents": [
    {
      "id": "payments",
      "title": "Payment on accept — THE LAUNCH BLOCKER",
      "priority": 1,
      "owns": [
        "src/screens/PayScreen.jsx",
        "src/lib/payments.js",
        "supabase/functions/create-payment-intent",
        "supabase/functions/stripe-webhook"
      ],
      "gates": [
        "#47g",
        "#155b"
      ],
      "defects": [
        "bookings_paid has been 0 for the life of the project — no card has ever been charged end to end",
        "counter-offer price shown vs charged (SPEC-211/212 shipped; needs live proof)",
        "FW-8 (founder walk 2026-08-02): enable Stripe TEST mode / dummy-card path so the booking E2E can be walked without real money ('enable card payments to pay with stripe ... or enable testing with dummy card')",
        "FW-9 (founder verbatim 2026-08-02, sharpens defect #1): 'user needs to pay when accepting a counter offer ... so their card is charged automatically... to eliminate the additional pay step... (funds kept until job done.. per spec release)'"
      ],
      "green_when": "one real booking reaches paid_at with a Stripe charge id"
    },
    {
      "id": "booking-loop",
      "title": "Request -> accept -> schedule -> complete",
      "priority": 2,
      "owns": [
        "src/screens/RequestScreen.jsx",
        "src/screens/InboundRequestScreen.jsx",
        "src/lib/api.js"
      ],
      "gates": [
        "#154",
        "#48",
        "#47i"
      ],
      "defects": [
        "FW-3 RESOLVED-AS-DATA (2026-08-03, live DB probe): request_responses.responder_id + services.owner_id are CONSISTENT — the 'Babysitter' listing is OWNED by the founder's own 'Tarik Sansal' account, so 'Tarik accepted' was factually correct. Residual UX hardening: the accept line should carry the SERVICE title next to the name so a self-test like this reads clearly. Do NOT change responder writes.",
        "core loop is auth-gated so it has only ever been verified in code, never walked live",
        "FW-1: no clear place to see my existing requests and their replies (founder verbatim: 'Add place to see existing requests and any replies')",
        "FW-2: intake accepted 'babysitter tonoight' and proceeded without a picked time — no typo nudge, no required time resolution",
        "FW-5: an old request re-fired by itself — tarik.sansal2 saw 'New request · House Cleaner · 28m' the founder never sent now; find what re-notifies old requests",
        "FW-6: default search toggle must be Paid Services for non-connectors (founder verbatim 2026-08-02)",
        "FW-13: /services/manage renders the wrong form/screens (founder verbatim + URL)",
        "FW-14: duplicated notifications and jobs — the same booking rendered twice ('H confirmed your time — pay to lock it in' at 7m AND 8m); dedupe by booking id, not by event row",
        "FW-15: 'Responses to your requests' rows carry no timestamp",
        "FW-10 (founder decision verbatim 2026-08-02): IG post story highlight 'should be mandatory not optional'",
        "FW-11: 'Copy link' must take to the SERVICE's profile, not the recommender's",
        "FW-12: provider is never notified when someone submits an IG review/post about them — tarik.sansal2 got nothing when t@cergio submitted; needs notify + review/approve step"
      ],
      "green_when": "a live walk completes the loop with two real accounts"
    },
    {
      "id": "crawl-sources",
      "title": "The 8 crawl sources",
      "priority": 3,
      "owns": [
        "supabase/functions/fulfill-crawl/index.ts"
      ],
      "gates": [
        "#179",
        "#180",
        "#181",
        "#182",
        "#187",
        "#205",
        "#239",
        "#240",
        "#243",
        "#244"
      ],
      "defects": [
        "google_sponsored still calls SerpAPI while VENDOR_OF_SOURCE claims apify — the spend meter is wrong on this source",
        "fulfillYellowPagesApify writes data_source 'yellowpages' instead of its rota name — the audit export's sample-100-yellowpages_apify.csv and the spend meter's per-lead count both read 0 beside its real rows (mechanism C). The SPEC-243 cap routes around it via AUDIT_CAP_SOURCES; the label itself is still wrong and needs its own micro-PR (relabel or dual-count, decide with history in view).",
        "NOT-A-DEFECT, standing order: yelp is PAUSED by the founder (2026-08-02, verbatim: 'don't delete yelp.. just pause as a source'). It stays in SOURCES_RR, the seeder and the dashboards with its rows kept, and stays OUT of CRAWLS_ONLY until the founder re-activates it. Do not 'fix' its silence, do not delete it, do not re-enable it — gate #239 enforces all of this.",
        "NOT-A-DEFECT, standing order: a source at SOURCE_AUDIT_CAP=100 leads is meant to be idle (founder, 2026-08-03, verbatim: 'add all sources not just creators to the crawl at 100 leads each max to review (except yelp)'). It stopped ITSELF; the founder audits sample-100-<source>.csv and names cleared sources. Do not 'fix' its silence, do not raise the cap — gate #243 enforces this."
      ],
      "green_when": "every paid source routes through Apify, or is parked with the reason recorded"
    },
    {
      "id": "creators",
      "title": "Creators as a first-class class",
      "priority": 4,
      "owns": [
        "scripts/growth-dedupe-queue.mjs",
        "scripts/_growth-scope.mjs"
      ],
      "gates": [
        "#202",
        "#204",
        "#208",
        "#237",
        "#245"
      ],
      "defects": [
        "follower band 5k-500k not enforced on the ig_services creator path",
        "prove LIVE that the is_business heal landed on the growth table and ig_services creator rows are being written (SPEC-237) — 262 service rows produced 0 creators while the reference healed and the table did not"
      ],
      "green_when": "100 creators from EACH source — ig-scraper-user-search (eq) and se:web-harvest (PREFIX) — then each source stops itself (SPEC-205/237)"
    },
    {
      "id": "spend-guard",
      "title": "No dollar without output",
      "priority": 5,
      "owns": [
        "scripts/growth-controls.mjs",
        "growth-controls.json"
      ],
      "gates": [
        "#185",
        "#189",
        "#190",
        "#207"
      ],
      "defects": [
        "ledger was blind to 78% of spend once; reconciliation must keep proving itself"
      ],
      "green_when": "ledger total matches the vendor account total within 5%"
    },
    {
      "id": "ops-dashboard",
      "title": "/ops/status — the founder's console",
      "priority": 6,
      "owns": [
        "src/screens/OpsStatusScreen.jsx",
        "supabase/functions/_shared/opsPayload.ts"
      ],
      "gates": [
        "#227",
        "#229",
        "#231",
        "#232",
        "#233"
      ],
      "defects": [
        "every count and CSV must obey City (DMA), Location, Category, Time and Rows — the LIVE counts bypassed all of them and made working filters look broken",
        "DMAs and every facet must come FROM THE DATA, never a hardcoded list",
        "deleting an export from opsPayload crashed the whole console with 'SOURCES is not defined' — no count, no filter, no DMA",
        "exactly TWO creator sources; a list of six put four permanent zeros beside the real ones",
        "google_sponsored is folded into google_lsa, never shown as its own row"
      ],
      "green_when": "changing any filter changes the counts, and every facet list is derived from live rows"
    },
    {
      "id": "data-dashboard",
      "title": "/ops/data — every dataset live + downloadable",
      "priority": 6,
      "owns": [
        "src/screens/DataExportScreen.jsx",
        "supabase/functions/leads-dashboard/index.ts"
      ],
      "gates": [
        "#203",
        "#210"
      ],
      "defects": [
        "screen counts have never been reconciled against a direct REST count"
      ],
      "green_when": "the screen total equals a direct count for the same filter"
    },
    {
      "id": "ci-health",
      "title": "CI honesty",
      "priority": 8,
      "owns": [
        ".github/workflows"
      ],
      "gates": [
        "#209"
      ],
      "defects": [
        "the e2e Playwright suite has been RED on main since roughly CI #11 and is advisory, so it never blocks a merge",
        "9 duplicate gate IDs baselined in qa-id-baseline.json"
      ],
      "green_when": "e2e is green and required, or deleted with the reason recorded — a permanently red advisory check is worse than no check"
    },
    {
      "id": "legal-copy",
      "title": "Securities language",
      "priority": 9,
      "owns": [
        "src/screens"
      ],
      "gates": [
        "#170"
      ],
      "defects": [
        "/rainmaker/apply carries securities-PARAPHRASE copy that gate #170 is blind to — it bans 5 exact phrasings, not the risk"
      ],
      "green_when": "legal review clears the copy, or it is de-securitised and the paraphrases are added to the gate"
    },
    {
      "id": "inbox",
      "title": "Jobs inbox — one truthful card per event",
      "priority": 3,
      "owns": [
        "src/screens/JobsInboxScreen.jsx"
      ],
      "gates": [
        "#154"
      ],
      "defects": [
        "FW-14 ROOT-CAUSED (2026-08-03, live DB probe): the x6 'pay to lock it in' rows were SIX REAL booking rows — incl. two exact double-tap duplicate pairs from the pre-SPEC-247 bug (server now blocks new ones; the two redundant twins 8dcb435f/b0e62600 were cancelled 2026-08-03). Residual: WATCH for new duplicate pairs (same consumer+service within 2 min, both unpaid) — any new pair means the server guard failed",
        "FW-15 FIXED IN CODE 2026-08-03 (response rows carry timeAgo) — verify live after deploy, then close",
        "FW-1: no clear place to see my existing requests and any replies (founder verbatim)"
      ],
      "green_when": "every inbox row maps 1:1 to a distinct live DB row, every row carries a timestamp, and the founder can find any request + its replies in one place"
    },
    {
      "id": "profile-ux",
      "title": "Profiles + service pages say WHO they are",
      "priority": 3,
      "owns": [
        "src/screens/ProfileScreen.jsx",
        "src/screens/ServiceDetailScreen.jsx"
      ],
      "gates": [
        "#211"
      ],
      "defects": [
        "FW-4: my own profile doesn't show my name; the provider's profile shows the REQUESTER's name (founder walk 2026-08-02; NOTE FW-3 turned out data-consistent — verify with the same DB-probe discipline before changing renders)",
        "FW-7: viewing a reply on the service profile must show a BOOK button carrying the COUNTER price — not the generic request CTA"
      ],
      "green_when": "every profile surface names its owner correctly and a countered price follows the user onto the service page"
    },
    {
      "id": "ig-verify",
      "title": "IG post verify + listing management",
      "priority": 4,
      "owns": [
        "src/screens/ServiceListVerifyScreen.jsx",
        "src/screens/RecoTrackingScreen.jsx",
        "src/screens/ManageServicesScreen.jsx"
      ],
      "gates": [
        "#48"
      ],
      "defects": [
        "FW-10 (founder decision verbatim 2026-08-02): IG post story highlight 'should be mandatory not optional'",
        "FW-11: 'Copy link' must take to the SERVICE's profile, not the recommender's",
        "FW-12: provider is never notified when someone submits an IG review/post about them — needs notify + review/approve step",
        "FW-13: /services/manage renders the wrong form/screens (founder verbatim + URL)"
      ],
      "green_when": "the IG verify flow enforces the highlight, links to the service, notifies the provider for approval, and /services/manage shows the intended form"
    }
  ]
}