# RUN 001 · SPEC-211 counter price + SPEC-212 crossPost dead

    agent        feature-builder
    branch       ship/spec-211-212        commit e8162b6d
    attempt      1 of 2
    verdict      PASS (static + build). NOT verified live.

## Criterion (founder, verbatim, 2026-08-01)
> "When viewing a reply to book, and seeing profile of the service, need to showcase the
> booking button with the price (from the counter).. not the generic request to book on
> profile"

## Scope — the wall
    src/screens/ResultsScreen.jsx
    src/lib/api.js            (SHARED — 2 lines, justified, no caller behaviour change)
    scripts/qa.mjs
    SCOPE.md
Nothing else was touched.

## What was wrong
    ResultsScreen.jsx:373   priceCents: cents        <- STICKER, used for the CHARGE
    ResultsScreen.jsx:384   price: counterCents...   <- COUNTER, used for the DISPLAY
    => card showed $5, booking wrote $35

    api.js:3761  crossPostRequest reads `request.city` with no `request` in scope
    => ReferenceError on EVERY call; "also notify others" 100% dead; sheet hangs busy

## What the agent found that was NOT in the brief
`Number.isFinite(+offeredPriceCents)` — `+null` is 0 and `Number.isFinite(0)` is true, so
a counter with NO price became an invented $0 quote, which after the fix would have been
CHARGED. It caught a bug its own fix would have created.

## Evidence
    node scripts/qa.mjs      195/195 pass, exit 0   (was 193, +2 gates)
    npx vite build           exit 0, 7.90s
    AST scope scan (acorn)   1 genuine unbound identifier in all of src/ — the one fixed
    mutation #211a           FAIL "priceCents: cents is the STICKER rate..."   restored -> pass
    mutation #211b           FAIL "+null is 0 ... invented $0 quote"           restored -> pass
    mutation #212            FAIL "crossPostRequest dereferences request..."   restored -> pass

## Not verified
No booking has been written at a counter price against production. Live proof outstanding.

## Reported, not fixed (out of scope, filed for a later run)
`api.js:3553` — `createRequestAndFanOut` reads `request.city`/`request.state`, which are
bound but are columns that do not exist on `requests`. Both resolve to null on every call,
so the SPEC-113 geo fallback is permanently dead on the main fan-out path.
