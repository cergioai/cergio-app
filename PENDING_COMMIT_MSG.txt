Find-a-Connector roster: ACCEPTED connectors only, with agreed price (SPEC-54)

Tarik 2026-06-18 — "only show confirmed connectors (this one is not
confirmed)." A seeded $25-countered row was leaking T onto info@cergio's
roster because the old gate counted offered/countered as "confirmed."

- BrowseConnectorsScreen confirmation gate now keys ONLY on
  status === 'accepted'. pending/offered/countered/declined/withdrawn/
  expired no longer qualify — a counter is an open negotiation, not a yes.
- The gate records the AGREED price per connector
  (offered_price_cents ?? official_price_cents ?? 0) in a Map, and the
  roster row shows that deal ("Free swap · accepted" or "Agreed $X/post")
  instead of the rate-card sticker (which wrongly showed $500 for a
  connector who'd accepted a free/$25 swap).
- Free-first sort + "no free connectors" messaging now key off the agreed
  price, not the rate card. Waiting-state copy updated (counters live in
  Spotlight requests > Sent, not the roster).
- FROZEN_SPEC SPEC-54 added; qa #54 added. All 57 qa flows green.
