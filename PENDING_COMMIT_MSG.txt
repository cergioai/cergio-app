Connector-aware paid-fallback copy on Results

Tarik: "when user is a Connector and asks for free services, we should
show them a msg (if there are no free services on offer...) that says
something like 'no free services... but here are best options...' (paid)"

The paidFallback plumbing already existed — when freeOnly=ON returns
zero, we re-query without it and show paid options under a banner.
The banner copy, though, addressed CONSUMERS only ("Free offers come
from Connectors. Ask a friend to join…") — Tarik's a Connector and
that line is nonsensical to him.

Fix: load the signed-in user's `cc_verified_at` flag alongside the
friends graph (same effect, no extra round-trip — `Promise.all` of
network + profile). Stash as `isConnector`. Render two copy variants:

  Consumer (existing):
    "No free {plumbers} nearby right now — showing paid options."
    "Free offers come from Connectors. Ask a friend to join, or
     pick a paid option below."

  Connector (NEW):
    "No free {plumbers} on offer right now — here are the best
     paid matches."
    "Free offers from other Connectors will show up here when they
     list one nearby."

No DB schema changes. No new RLS surface — `cc_verified_at` was
already readable on own profile.

QA: 41/41 pass.

Files:
  src/screens/ResultsScreen.jsx
