Profile: Recommendations-received + Services-received sections (SPEC-49f)

Tarik 2026-06-18 — full-profile additions.

- "Recommendations received" — consolidated header + list of every reco
  RECEIVED across the profile's services (recosReceived, RecoRow). The
  SPEC-49 "People who love {name}" surface as its own section, alongside the
  per-service inline recos. Collapses when none.
- "Services {name} has received" — services the profile has used on Cergio
  (completed/confirmed/in-progress bookings where they were the consumer,
  deduped, newest first, ServiceTile). Collapses when none.
- Section order: signal block → Spotlights → Services → Recommendations
  received → Services received → Go-Tos.
- All sections use design-spec tokens only (bg-cream / DM Sans scale / b3 /
  bdr) — no arbitrary colors or fonts.
- FROZEN_SPEC SPEC-49f added; qa #49 extended. All 57 flows green.
