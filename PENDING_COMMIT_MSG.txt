Spotlight request (Connector's view): counter on free swaps, richer provider counts, grammar + scheduling note

- Counter is now available on FREE swaps too (not just paid) — the Connector can
  counter "I'll do it for $X" via the same price composer ("Counter with a price").
- Lead stays the SERVICE (title + reco's received) — that's what drives the
  Connector's yes — then a Connector badge (if the requester is also a Connector)
  + a richer count line: network on Cergio · reco's made · IG · TikTok.
  getSpotlightRequest now hydrates tiktok_handle/followers + cc_verified_at.
- Headline grammar: "Free {Service} Offer  ⇄  Free {IG/TikTok} Spotlight".
  Ask copy fixed: "in return for a free {platform} spotlight" (was "a Instagram").
- Scheduling note: accepting agrees to the swap; the two parties arrange timing
  together afterward — explicitly different from a Connector requesting a free
  SERVICE (where the time is set up front).
