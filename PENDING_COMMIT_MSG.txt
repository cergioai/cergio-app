design(earnings + browse): cash/credit + ripple-back + GPI network note + (i) explainer + no-free-Connector banner

Earnings animation
  Tarik (one message): "add small note that connectors get cash and
  normal users get credit... arrows back from the connector's audience
  to the services... or other to show the effect of spotlight... add
  small note on GPI (relative to participation / connector's network
  growth..)".

  Scene 1 — cash vs credit footnote
    Two pills below the $250 payoff:
      [ Connectors earn CASH ]   [ Users earn credit ]
    Mint pill for Connectors, neutral for Users — soft, no shouting.
    Lands at 3.4s after the verbs settle.

  Scene 2 — ripple-back arrow (audience → new customers)
    A dashed orange curve sweeps from the audience cluster on the left
    BACK up to the Provider tile, labelled "new customers" along the
    vertical. The spotlight isn't a one-way payment — it's a loop:
    audience → bookings → revenue to the provider, which is why the
    provider was willing to barter in the first place.

  Scene 3 — GPI network-growth note
    Small line above the punchline footer:
      "Tied to YOUR participation × YOUR network's growth"
    Two-fact stack ensures users read both:
      • the math (participation × network)
      • the timing (earlier joiner = bigger ripples).

Connector explainer (Home + EarnExplainer)
  Tarik: "add a connector's explainer somewhere (perhaps a small i that
  pops up...(also on homepage).. to take to /rainmaker/apply".
  - Home: tiny (i) chip next to the "Free for Connectors" toggle in
    the search box (only when the toggle is active). Tap → /rainmaker/apply.
  - EarnExplainerScreen: green "i What's a Connector? Apply to become
    one →" link under the page subtitle. Tap → /rainmaker/apply.
  Both keep the existing big "Invite your clients → become a
  Connector" promo block — these are just additional curiosity
  on-ramps.

Browse Connectors — "no free Connector right now" banner
  Tarik: "add a msg if there's no free connector to say (and that
  below are some connectors who'll spotlight for a fee)".
  When the loaded Connector list contains zero rows with BOTH
  spotlight_price_*_cents IS NULL, render a soft warn banner above
  the list:
    "No free Connectors available right now"
    "All of the Connectors below set a fee per spotlight post — but
     it's often negotiable. Ask for a lower price (or a free post in
     exchange for your service) inside the request."
  Banner disappears the moment any free/swap-only Connector is loaded.

Verified: qa.mjs 41/41 · JSX syntax check on every touched file: green.
