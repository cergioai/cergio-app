fix(invite/reco): real seeded contacts + visible CTA + spotlight audit seed

InviteFriendsScreen (/invite/friends)
- Tarik: "button at bottom is hidden... populate with test data instead
  of hard coded data".
- Hard-coded CONTACTS mock dropped. Pool now loads via
  listInvitableProfiles() — real profiles from the DB (seeded Alex /
  Connie / Sam / Maria / Sofia / Henry / Jackie / Penny + any signups),
  excluding the signed-in user.
- Connector badge surfaces on contact rows whose profile has
  cc_verified_at NOT NULL.
- CTA footer was `fixed bottom-0` inside a `pb-24 overflow-hidden`
  container — on some viewports the fixed positioning + parent overflow
  clipped the button below the visible area. Switched to
  shrink-0 / mt-auto inside the flex column so the CTA is always the
  last block in the layout and CAN'T be covered. Loading + empty
  states render visible "Pick at least one contact" copy in the footer
  so it never reads as "broken".

InviteSelectedReviewScreen (/invite/review)
- Same swap: pulls picked contacts from listInvitableProfiles instead
  of CONTACTS mock, so the names that appear here match the rows the
  user actually tapped on the previous step.

RecommendServiceFormScreen (/invite/recommend)
- Tarik: "wire this to contacts (seed with fake data..)".
- Autosuggest pool switched from CONTACTS mock → listInvitableProfiles
  with deterministic synthesized phone + email per profile (305-555
  numbers + name@cergio.test). The form flow (autofill on pick → send)
  still works because the synthesized phone/email are accepted by
  notify-user and the recommendations insert.

api.js — listInvitableProfiles
- New helper: pulls profiles, excludes current user, attaches
  is_connector + a stable AvatarLink-style avatar_seed + synthesized
  phone/email so the same row works in both pickers (Invite/Reco) and
  the form (autofill).

Spotlights + free-services audit (Tarik: "audit the spotlights free
services")
- Free services: end-to-end path is implemented and live.
  Sofia's "Deep Clean (Connector perk)" offering has price_cents=0 →
  PDP renders "Free for Connectors" pill → handleBook detects
  price_cents=0, skips Stripe, auto-confirms. No fix needed.
- Spotlights: code paths are all wired (RequestSpotlightModal →
  createSpotlightRequest → ConnectorRequestsScreen → counter / accept
  → SpotlightPaymentModal → markSpotlightPosted → confirmSpotlightPost).
  Gap is ONLY data — no seeded Connector had spotlight_price_*_cents
  set, so /connectors/browse showed the EmptyState
  ("Connectors are setting their spotlight rates..."). Added the
  helper `Seed Connector Spotlight Rates.command` that sets:
    Connie  — IG $250/post + TT $180/post (handle connieconnects, 4.2k)
    Jackie  — IG $150/post
  After running it, /connectors/browse renders real rows + the request
  flow is demoable end-to-end.

Verified: qa.mjs 41/41 · JSX syntax check on every touched file: green.
