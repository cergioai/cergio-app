/inbound: connector bio + raw request text; scaffold pre-booking Q&A

- Added a small Connector bio under the "See full profile" link (profiles.bio
  / headline), pulled in getInboundRequest.
- Message now derives from the requester's raw request text (description /
  query) rather than the parsed type.
- Scaffolds the pre-booking Q&A feature: migration request_questions (table +
  RLS) and an Apply command. The Ask UI + API are wired in the next push,
  after the migration is applied.
