Recommendations come from a completed booking (SPEC-53 rewrite)

Tarik 2026-06-17 — recos are now gated on a real booking; the rate+post
flow is the single recommendation mechanism:

- Removed the standalone "Recommend {name}" button + RecommendProviderModal
  usage from ServiceDetailScreen (today's earlier SPEC-53). Recos no longer
  happen from the service page.
- MarkBookingPostedModal now serves BOTH free and paid bookings:
    • IG post REQUIRED for free/barter, OPTIONAL when paid (isPaid).
    • A 4★+ submit writes a service-linked recommendation (recommendService)
      so it shows on the provider's profile + the recommender's Go-Tos.
    • Copy/labels adapt ("Rate & recommend" / optional post link / button).
- Inbox Overview: added "Rate & recommend" for PAID completed bookings
  (time-boxed 14d) alongside the free "Rate & post".
- EXCEPTION preserved: the invite-reco (RecommendServiceFormScreen, no
  booking) that onboards a new provider — SPEC-49d.
- FROZEN_SPEC SPEC-53 rewritten; qa #53 updated. #47/#49/#50/#53 green.
