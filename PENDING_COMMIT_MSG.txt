/inbound: service in headline, task in message, expandable map, named greeting

Tarik 2026-06-14:
- Headline now leads with the actual service: "Free {service} ⇄ Free spotlight
  to {N} followers" (dropped the generic "service request"); removed the now-
  redundant summary line.
- Message carries the TASK, not the service-type label: composeNote uses the
  request description (e.g. "cook vegan for my wife's birthday") instead of
  "need a personal chef". Service type lives in the headline only.
- Greeting uses the provider's first name when available — "Hi Jane,"; falls
  back to "Hi!" when the account name is generic. Broadened the name source.
- Map is tappable and opens an Airbnb-style expanded view (larger map, still
  approximate area, no precise pin; exact address blocked until confirmed).
