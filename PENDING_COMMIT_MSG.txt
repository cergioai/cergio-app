Profile: drop network from service facet too (SPEC-49b follow-up)

The Cergio-network count was still showing on BOTH the service and
connector facets (duplicated). Per Tarik, IG AND network are connector-
side signals that belong around the Connector badge, not on a service
(e.g. a plumber). formatKeyCounts gains an includeNetwork option
(default true; existing callers unchanged); the profile's service facet
now passes includeReach:false + includeNetwork:false, leaving only the
always-on mutuals line under "role · N recos received". FROZEN_SPEC
SPEC-49b updated. qa #49 green.
