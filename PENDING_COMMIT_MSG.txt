SPEC-47g held-release (flag-gated) + SPEC-63 crawl monitoring & alerts

SPEC-47g (staged, OFF behind HOLD_RELEASE_ENABLED): escrow money model —
create-payment-intent branches instant (destination charge) vs held
(transfer_group); webhook records charge + defers earnings in held mode;
markBookingComplete sets release_due_at=+3h with early-completion guard
(release_requires_confirm) + confirmJobDone; release-funds worker transfers
provider share from the original charge, idempotent; migration adds columns
+ documented cron; Release Due Funds.command.

SPEC-63 crawl pipeline self-monitoring (can't fail silently):
- crawl-health-check edge fn (service-role; cron + Crawl Health Check.command)
  classifies STALLED/FAILED/EMPTY and emails admin a diagnosis via Resend.
- admin-crawl-status edge fn (admin-allowlist gated) + /admin/crawls
  (AdminCrawlScreen, auto-refresh 60s) live dashboard via link.
- Monitor Crawl + Outreach.command for terminal view.
Deploy script ships all new fns. FROZEN_SPEC + qa (#47g, #63) → 67 flows pass.
Not live until functions deployed (+ HOLD_RELEASE_ENABLED for 47g).
