feat(activity+earnings): compact feed cards + share buttons + referral source breakdown

Tarik 2026-06-03:
  (1) "clean up activity feed UX here.. the information is all far
       from each other.. need service in same line as the action
       taken (your friend Jane spotlight service V to followers...
       Connector G announced a dog walking service etc...)"
  (2) "add share buttons on content on reco's on services"
  (3) "show earnings breakdowns on referrals (to see where each
       came from)"

═══════════════ (1) Activity feed compaction ═══════════════

GoatShareCard restructured so WHO + WHAT + TO-WHOM all live in
one sentence at the top of the card:

  [avatar]  your Connector Mia Influencer spotlighted
            Tomas Dog Walker (Dog Walker · Miami, FL) to
            92,000 followers                              [⇧ Share]

Photo dropped below as pure decoration. The old layout split
"Tomas Dog Walker / Dog Walker · Miami, FL" at the top and
"Spotlighted by your Connector Mia to 92k followers" pill below
the photo — the eye had to bridge the photo to connect them.
Now they read as one sentence.

ListingCard given the same treatment — single-line sentence
("Owner listed a new service: X (Type · City)") + share button.

═══════════════ (2) Share buttons ═══════════════

  • shareItem({ title, text, url }) helper — Web Share API
    first, clipboard fallback (matches ResultsScreen pattern).
  • ShareIconButton component (white pill + b2 upload icon) —
    one tap on the card.
  • Wired on GoatShareCard (spotlights) and ListingCard
    (new-service announcements). Forwarded text includes the
    full sentence + the canonical service URL.

═══════════════ (3) Earnings breakdown on Referrals tab ═══════════════

Each row now leads with the friend who triggered it:

  • Direct ($250): headline "<Friend name> booked"
                   pill: Direct
  • Chain ($12.50): headline "Chain payout via <inviter friend>"
                    sub-line: "Friend-of-friend booked (<fof name>)"
                    pill: Chain +5%
  • Spotlight: "Instagram spotlight" / "TikTok spotlight"
  • Booking (own service): "Service booking"

Pulls from earnings.meta — `tier` ('direct'/'fof') and
`friend` ('Alex' for direct, 'Sam->FriendA' for chain). When
meta is sparse (no friend name), falls back to generic copy
that still reads honest.

═══════════════ Files ═══════════════

  cergio-app/src/screens/ActivityScreen.jsx
  cergio-app/src/screens/EarningsScreen.jsx

═══════════════ Verified ═══════════════

  scripts/qa.mjs — 41/41 PASS
  @babel/parser JSX on both files — OK.
