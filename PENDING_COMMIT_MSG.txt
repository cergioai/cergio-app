Frame 3 alignment to Figma + flow map source-of-truth (SVP)

Aligns /inbound (RequestFromConnectorScreen) to the Figma "Message — Essential
Details" frame, per Tarik's decisions:

- Header now matches Figma: back · {name} · ••• (opens full profile).
- Status row: "Needs Response" + "View Details" (scrolls to job details).
- "See full profile" kept (moved onto the requester block, not removed).
- Frame 2 ("{Connector} wants to market your services") folded into the top
  banner, now naming the Connector's follower reach.

Also adds SVP_FLOW_MAP.md — the design-accurate, frame-by-frame map of the
"Accepting Free Service request" column (read from the Figma file), the
terminology translation (Romio→Cergio, GOAT→Connector, Go-To→Reco'd), the
quarantine plan (src/_quarantine + qa guard so duplicates can't render), and
the build order. This file is the working source of truth for the rebuild.

Decisions captured: /request/:id stays (requests can be free OR paid via the
homepage search toggle); frame-2 folded; header matches Figma + keeps See
full profile.
