feat(claim): real claim-profile flow for recommended providers (#1)

The "Claim your profile" link wrongly pointed at the recommender's /u/ page with
no claim UI. Now it lands on a real claim flow:

- RecommendServiceFormScreen: reco-by-phone deep_link → /claim?by=<recommender>&
  as=<serviceType> (was buildInviteUrl(recommender)).
- New ClaimProfileScreen + /claim route: "you were reco'd as X by Y → Claim your
  profile". Signed out → /auth (signup; phone mandatory) with returnTo back to
  /claim. Signed in → auto-attaches the pending recommendations made to this
  phone and lands them on their profile / list-a-service.
- api.claimRecommendations() → claim_recommendations() RPC (migration
  20260626010000): SECURITY DEFINER, phone-matched (last-10 digits, format-
  insensitive) — sets recipient_id on recommendations where recipient_phone
  matches the caller's signup phone and recipient_id is NULL. Scoped to the
  caller's own phone.

Requires "Apply Pending Migrations" for the RPC (TWO pending now: claim_
recommendations + connector_invite_grant). All 75 qa flows pass; files
esbuild-clean.

Batch status: #1 ✓ #2 ✓(deployed) #3 ✓(already) #4 ✓(deployed) #5 ✓ #6 ✓.
Held: #75 Home/Inbox Airbnb — pending a proper profile Airbnb redo (#84).
