Barter: provider Mark-job-complete + rate-with-post + easy action surfacing (SPEC-47c)

- bookings.completed_at (migration applied) + markBookingComplete(): provider marks the job
  complete anytime → nudges the Connector to post (notify-request job_complete; email + in-app,
  SMS later). Distinct from post_confirmed_at; for paid it starts the auto-release window.
- Inbox Upcoming "Jobs for you": provider gets a "Mark job complete" button; once complete the
  Connector's card upgrades to a prominent "‹Provider› marked the job complete — post now" prompt.
- Rate + post in ONE step (MarkBookingPostedModal): star rating required. 4★+ → createReview +
  markBookingPosted publishes the spotlight. Below 4★ → post HELD; Connector explains; review is
  saved + framed as PRIVATE ("truthful reviews from trusted friends, not gamed reviews from
  strangers") and shared with the provider. (Provider reply/escalate/admin = next module.)
- Overview action rows make the notifications easy to find: "Post your IG spotlight · N" and
  "Spotlights to review · N".
- FROZEN_SPEC SPEC-47c. (Feed post thumbnail + Previous-spotlights population = next.)
