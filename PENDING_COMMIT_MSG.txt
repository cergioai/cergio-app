Guardrails + money-flow schema fix + test plan + 8 new invariants

The 2026-05-28 audit (Chrome MCP probe as Tarik) found that the
community money flows have been silently broken for weeks:

  • notifications table — DOES NOT EXIST
    → every notifyUser() has been a silent no-op
    → providers never see new requests
    → entire "ping the network" UX has been theatre
  • connector_applications table — DOES NOT EXIST
    → tapping Become-a-Connector submit was theatre
  • recommendations.inviter_id column — MISSING
    → reco attribution couldn't write
  • profiles.tiktok_handle/_followers/_verified_at — MISSING
    → ProfileScreen quietly fails on the TikTok row
  • Tarik's two own services have taxonomy_provider_type=NULL
    → can't be searched or notified
  • invites + earnings tables are empty
    → no money flow has fired end-to-end, ever

SCHEMA FIX

New migration 20260528000000_money_flow_schema_fix.sql creates the
4 missing tables/columns with proper RLS. Applied to live Supabase
via PAT (HTTP 201 confirmed).

TEST INFRASTRUCTURE

  TEST_PLAN_MONEY_FLOWS.md — rigorous plan: §0 findings, §3 seven
    flows with assertions (signup-with-ref, search→notify, bid-back,
    recommend, connector apply, spotlight barter, first-booking
    credit), §4 invariants 26–30, §5 UX queue, §6 execution order

  Apply Money Flow Migration.command — applies the schema (idempotent)
  Seed Money Flow Test Data.command — plants Alex, Connie, Penny,
    Sam personas + Penny's listed Plumber service + Connie's verified
    connector_application
  Fix Penny Service.command — backfills Penny's service when the
    first seed run hits the bad-column edge case

BUILD VERSION PILL — observability

  BuildVersionPill.jsx in bottom-left of every screen. Shows current
  short git SHA + build time. Click to copy. Vite define{} injects
  SHA at server-start. Was the single missing observability piece
  during the 2-day search debug — HMR-stale-closure bugs are now
  visible at a glance.

INVARIANTS — qa.mjs grows 17 → 25 (all green)

  #18  Build pill wired
  #19  saveAddress writes user_metadata FIRST  (locks task #103)
  #20  signUp never strands user                (locks task #102)
  #21  Addresses Google-verified before save    (locks task #85)
  #22  No "Cergio Coin/Cash" in signed-in copy  (locks task #78)
  #23  Every Profile navigate target resolves
  #24  Rewards copy uses REWARDS constants — IMMEDIATELY caught a
       hardcoded "$250" + "$10,000" in RainmakersScreen, fixed in
       this same commit. Added REWARDS.milestoneFriends / milestoneBonus.
  #25  Connector apply has side-by-side + compounding + selector + GPI

CRITICAL_FLOWS.md §14–§21 added with plain-language rules + qa#
cross-refs.

OUTSTANDING (sketches in OVERNIGHT_STATUS.md):
  • F1–F7 live execution against seeded personas (blocked on Chrome
    MCP per-domain permission)
  • Leaf logo "pulse intensity" prop
  • SRP slow-load tied to real notifications/bids counts
  • Login elegance, house ad softening, profile font reduction
  • Home copy consistency one-pass audit

VERIFIED  qa.mjs: 25/25 pass
          Live DB: notifications + connector_applications tables exist
          Personas seeded; Penny's Plumber service listed at 25.81,-80.13

Files:
  src/components/ui/BuildVersionPill.jsx          (new)
  src/App.jsx
  vite.config.js
  src/lib/rewards.js
  src/screens/RainmakersScreen.jsx
  src/screens/ResultsScreen.jsx
  scripts/qa.mjs
  CRITICAL_FLOWS.md
  TEST_PLAN_MONEY_FLOWS.md                         (new)
  supabase/migrations/20260528000000_money_flow_schema_fix.sql (new)
