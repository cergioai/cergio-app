feat(profile): user headline + bio, rendered on Profile + PublicProfile

Tarik 2026-06-05: "add ability for users to add a headline and a bio."

Schema:
  • New profiles.headline (text, ≤120c) via idempotent migration
    db/migrations/2026-06-05-profile-headline.sql.
  • profiles.bio already exists (in schema-v1.sql) — no schema change.

Migration runner:
  • Apply Profile Headline Migration.command
    Loads DATABASE_URL from cergio-app/.env.local and runs psql -f.
    Idempotent — safe to re-run.

EditProfileModal (used by ProfileScreen → Account & settings →
View and edit profile):
  • Two new fields: Headline (120c cap, single line, char counter)
    and Bio (500c cap, 4-row textarea, char counter).
  • Saves to profiles via supabase.from('profiles').update — RLS
    already pins UPDATE to auth.uid() = id.
  • If the headline column hasn't been migrated, the error message
    is surfaced verbatim with a "run the migration" hint so Tarik
    knows to run Apply Profile Headline Migration.command. The
    user's typed copy stays in the form for a retry.
  • Display name + phone continue to save via auth.updateUser.

ProfileScreen hero:
  • Pulls profiles.headline on mount; renders it as a one-line
    subtitle between "Hi {firstName}!" and the Connector/Member pill.
  • Defensive fetch — column-missing errors are swallowed.

PublicProfileScreen:
  • Profile SELECT now includes headline.
  • Headline renders beside the display name (below it on mobile,
    above the role/Connector pill row) so viewers see the user's
    own positioning before the platform-derived badges.
  • Bio continues to render in the existing About section.

Verified: scripts/qa.mjs 41/41 PASS · JSX parses clean on all 3 files.

Next iteration (queued, not in this commit):
  • Profile IA — per-section counts + global header (#196).
  • Spotlight request rows — route to service profile, cleaner msg,
    Sent-side action-set fix (#194, #195).
