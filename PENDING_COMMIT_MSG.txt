design(#50): PDP secondary CTA radius matches spec (rounded-[24px])

- "Leave a go-to review" full-width button was rounded-pill; design-spec
  reserves pill for chips/filter-pills and mandates rounded-[24px] for
  full-width CTAs. Now matches the primary "Request ..." sticky CTA.
- Audit pass: service PDP + public profile screens verified token-clean
  (colors/typography per design-spec); SEO already covered (in-app
  useDocumentMeta + SSR meta SPEC-61/62). All 71 qa flows pass.
