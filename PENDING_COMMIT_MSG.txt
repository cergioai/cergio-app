SEO part 1: per-record document meta on profile + service PDP (SPEC-61)

Tarik 2026-06-19 — SEO (full prerender chosen; this is the safe Part 1).

- New useDocumentMeta hook: sets per-record <title>, description, canonical,
  Open Graph + Twitter tags; restores title on unmount; gated by `ready`.
- Wired on /u/:profileId (name + headline/bio) and /service/:serviceId
  (listing + owner + cover image). Hooks called before early returns.
- index.html now carries default brand meta (description/OG/canonical) the
  screens override.
- Helps Google (renders JS) + correct tab/share titles. Full social-scraper
  cards (FB/LinkedIn/iMessage) need SSR — Part 2, a separate build-verified
  Vike pass (can't ship as client-only tags).
- FROZEN_SPEC SPEC-61 added; qa #61 added. All 64 flows green.
