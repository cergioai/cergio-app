Rebuild /inbound from the Figma frame (1:1 treatments) + real map

Stops patching the old components; rebuilds the connector-request screen from
the Figma "Message (Essential Details)" frame so it actually matches the design:

- "Needs Response" + "View Details" as icon+text (not green pills)
- Large display title; "Free for Connectors" as inline green shield+text
- Real Google Static Map (VITE_GOOGLE_MAPS_KEY + request lat/lng) under the
  approximate-area radius + white info box + eye-off pin (falls back to the
  cream card if the tile errors)
- "Scroll down for messages" + requester block (IG handle/followers, Connector
  badge, strength, coral See Instagram) + reserved IG photo strip (real media
  only) + See full profile
- Benefit line + green "Accept free request" with Counter / Decline
- Shows the actual service needed (service_type + description + when)
- Removed the "wants to market your services" banner (not in the Figma frame)

Figma = layout source of truth; overrides (Connector badge, friends-in-common,
strength) sit on top. Iterating to exact colors next.
