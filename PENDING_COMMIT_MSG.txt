Reward-flow animation embedded as /earnings/how + qa #31

User wanted the 6-step business-model explainer rendered as a real
screen, not just a widget preview in chat.

WHAT SHIPPED

  src/components/ui/RewardFlowAnimation.jsx (new)
    React port of the chat widget. Self-contained:
      • Counter at top — "Your network earnings" — ticks on step change
      • SVG scene that grows: sprouts emerge, rain falls when revenue
        lands, fruits appear, trees mature when Connector mode kicks in,
        orchard at the end
      • Caption (step num + title + body) under the scene
      • Controls: Back / progress bar / Next / (auto-replay at end)
      • Auto-advances 4.5s per step; manual control pauses auto-advance
    Reads all numbers from REWARDS so per-friend ($250), friend-of-
    friend ($12.50 = 5%), and Jamie example (50 friends → $12.5K)
    stay canonical.

  src/index.css
    New keyframes for rf-drop (rain), rf-fruit (bobble), rf-pop
    (coin entrance), rf-sway/-b/-c/-d (per-age sprout sway), rf-tick
    (counter update flicker). All scoped under rf- so they can't
    collide with the existing cg-* leaf animations.

  src/screens/EarnExplainerScreen.jsx
    Animation embedded at the top of the body. Existing copy
    (7% fee, "the model", "example", Growth Participation Income
    teaser, mission tagline) tightened so the animation does the
    heavy lifting and the prose is the footnote.

  scripts/qa.mjs #31
    Locks:
      • RewardFlowAnimation export exists in the expected file
      • All 6 steps are present (Step 1..6, friend-of-friend, Connector)
      • EarnExplainerScreen imports + renders it

  qa.mjs: 31/31 pass.

ANY CALL SITE CAN EMBED IT
  Drop <RewardFlowAnimation /> anywhere — Home invite popup, onboarding
  walkthrough, marketing landing — same component, same numbers.

Files:
  src/components/ui/RewardFlowAnimation.jsx   (new)
  src/index.css
  src/screens/EarnExplainerScreen.jsx
  scripts/qa.mjs
