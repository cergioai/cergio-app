feat(invite): Connectors-invite-Connectors + captivating invite copy (#5, #6)

#6 — never share a bare link. The "copy link" buttons now copy a captivating
message + link (InviteFriendPopup, FindFriends, Rainmakers). Share sheets
already had copy; the bare clipboard copies were the gap Tarik hit.

#5 — Connectors invite Connectors (explicit, guarded):
- buildConnectorInviteUrl() → the short link + `?c=1`. Capture persists a
  `connector` flag (captureRefFromUrl + storeRef); isConnectorInvite() reads it.
- On signup, recordInviteFromActiveRef calls grant_connector_from_invite(inviter)
  when the flag is set. SECURITY DEFINER migration
  (20260626000000_connector_invite_grant.sql) GUARDS it: grants Connector to the
  caller ONLY IF the inviter is a verified Connector (a forged ?c=1 from a
  non-Connector grants nothing) and the caller isn't already one.
- "Invite a Connector →" button on RainmakersScreen, shown only to verified
  Connectors (getMyCcStatus), copies the connector-invite message + link.

Also shipped this batch (separate concerns): #2 SMS-send fix + #4 claim-email
referral copy (notify-user, already deployed); #3 phone-mandatory verified as
already implemented (required tel field + phoneValid gate + signUp stores it).

Requires migration apply ("Apply Pending Migrations") for the grant RPC.
All 75 qa flows pass; changed files esbuild-clean.
Remaining from the batch: #1 claim-profile flow (largest — next increment).
