fix: 3 regressions — geocoder banner, barter pill, invite contacts

CERGIO-GUARD fixes (2026-06-11):

1. google.js — Nominatim rescue now clears geocode lastError so
   SetupCheckBanner doesn't show REQUEST_DENIED when fallback succeeded.
   Auth errors (kind='auth') still surface as before.

2. ResultsScreen.jsx — removed "Sent to Connectors near you · they
   barter for $250 in free spotlights" pill. It appeared on the
   Connector-looking-for-service path and showed wrong copy.
   Canonical waiting state is the leaf + "We'll let you know when
   offers land." block (unchanged).

3. api.js + InviteFriendsScreen.jsx — listInvitableProfiles() now
   scopes to the user's network table (followed_id) instead of dumping
   all profiles. Empty network → empty list, no seed data shown.
   Removed pre-selection of first 2 contacts on load.
