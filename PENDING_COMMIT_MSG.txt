design-qa: capture request_detail + inbox + activity (booking-purpose surfaces)

Phase 6 follow-up: the prior commit shipped the booking-purpose
context banner/eyebrow on three screens, but the screenshot-qa
harness didn't visit any of them. Without coverage, the next QA
gallery would silently regress those surfaces.

Added three rows to SCREENS in scripts/screenshot-qa.mjs:

  key              route                                           section
  ────────────────────────────────────────────────────────────────────
  request_detail   /request/795c2713-463a-458c-9cd1-67e1e5255f64   § 5.9
  inbox            /inbox                                          § 5.9
  activity         /activity                                       § 5.9

The request_detail route hits a real seeded UUID; falls back to the
FALLBACK demo shape if Supabase 404s, so the capture is always
non-empty.

Gallery now captures 13 screens (was 10). Vertical-baseline
tolerance per § 7 stays at 2px. design-qa/index.html + the three
new PNGs go in this commit so the gallery state in git matches
what's in Tarik's browser right now.

Verified: re-ran Screenshot QA.command end-to-end after the edit,
gallery rendered 13 rows at 1:17:37 AM 2026-06-01.
