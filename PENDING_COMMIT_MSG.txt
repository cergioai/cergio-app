/inbound: provider can ask pre-booking questions (Q&A)

Wires the request_questions backend (migration applied) into the screen:
- api: askRequestQuestion / listRequestQuestions / replyRequestQuestion
  (+ best-effort notify-request 'question' event).
- Provider "Ask a question before you accept" action with preset chips
  (Who buys ingredients? · Cover food costs upfront? · Send an ingredient
  list?) + free text. Sends to the requester; the Q&A thread renders on the
  screen (question + reply / "Waiting for reply…").

Requester reply surface (JobsInbox) lands in the next push to close the loop.
