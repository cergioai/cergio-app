SPEC-48c: party-signal RULE — a service viewing a Connector LEADS with the Connector badge

Tarik 2026-06-15: "make it a rule — lead with the same info next to each user type everywhere,
respective priority." The /inbox free-service + booking cards were missing the Connector badge.

- getInboxPartyCounts now returns isConnector (via isConnectorProfile) alongside the counts.
- JobsInbox free-service "New requests near you" + booking cards now LEAD with the Connector
  badge next to the name, then IG · network · reco's made (reach-led). Status pill demoted to subtle.
- Rule frozen in FROZEN_SPEC SPEC-48c: service-viewing-Connector → Connector badge + reach;
  Connector-viewing-service → service type + reco's received first. qa #48 asserts the flag + badge.
