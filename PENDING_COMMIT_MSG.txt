SPEC-84: redo profile Airbnb pass (lead identity card + card surfaces)

Tarik flagged the profile's Airbnb application as poor / "very far from
Airbnb fonts, distances, UI". Rebuilt the lead identity card
(ProfileSignalBlock) and softened the card surfaces to match the
design-spec "Airbnb elegance standard".

- Lead identity card: heavy border-bdr/p-4 -> hairline border-line/p-5,
  mt-2 -> mt-3.
- Readable type ramp: headline + bio now body (14), not meta (12/13);
  reach stays body-bold black; mt-3 (12px) rhythm between distinct blocks.
- Replaced the cramped meta-sm network/recos line with the SHARED inline
  TrustStream primitive (bold number + quiet label) so the profile reads
  identically to the PDP / request previews. Mutuals stay the one NAMED
  green trust line below.
- "See Instagram", service-type and mutuals lines bumped to body-sm.
- ServiceTile + Go-To cards + empty state: border-bdr -> hairline
  border-line, reading text 12 -> 14, p-4 -> p-5; Spotlights section gets
  the hairline top divider for consistent section rhythm.

No data/behavior change (same fields, same order, same primitives).
QA: all 75 flows pass.
