Recommendations: write + count (Option C) — locked with qa #27

OPTION C from the 2026-05-28 reco-schema decision: keep the
recommendations table, wire writes on send, surface a real "Recs
sent" counter on EarningsScreen.

WHAT CHANGED

  RecommendServiceFormScreen.submit:
    On successful notifyUser, ALSO INSERT a row into recommendations:
      recommender_id  = me
      inviter_id      = me (attribution anchor)
      recipient_phone = picked.phone
      service_id      = null (free-form for now)
      message         = blurb
    Best-effort — failed insert warns to console; UI still toasts +
    routes to /earnings. The notifyUser send is the source of truth
    for delivery; the table row is the analytics anchor.

  EarningsScreen:
    New state — invitesCount + recsCount.
    Effect pulls both via head:true count queries on mount:
      invites           WHERE inviter_id   = me
      recommendations   WHERE recommender_id = me
    The empty-state "friends invited" + "services reco'd" pills now
    render the LIVE counts. No more hardcoded "0".

  qa.mjs #27 — Recommend writer + Earnings reader:
    Statically asserts that RecommendServiceFormScreen calls
    .from('recommendations').insert(...) with recommender_id, and
    that EarningsScreen .select()'s from recommendations into a
    recsCount state variable.

  Run Money Flow Tests.command — F7b updated to use real schema
    (recommender_id, recipient_phone, message). Was a soft warning
    before; now a hard PASS.

VERIFIED  qa.mjs: 27/27 pass

NEXT (task #172 — already on the board):
  Write side wire-up: useChat.submit creates a `requests` row and
  fans out notifications to matching providers. This closes the
  loop so a real submit triggers F2's notify fan-out (we proved the
  persistence layer works; now connect the UI write to it).

Files:
  src/screens/RecommendServiceFormScreen.jsx
  src/screens/EarningsScreen.jsx
  scripts/qa.mjs
  Run Money Flow Tests.command  (parent folder)
