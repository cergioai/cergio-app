Referral payout integrity + invite-loop notifications + tracking $ (SPEC-57)

Tarik 2026-06-18 — "make sure users get paid for referrals"; invite tracking
is the spine.

MONEY FIX:
- A FREE ($0) first booking no longer BURNS the referral. creditInviterOnFirst
  Booking returned early-stamped first_booking_at at $0, locking the inviter
  out of ever earning from that friend's later PAID bookings. Now a $0 booking
  is a no-op (no stamp, no row) so the 7%-capped reward lands on the friend's
  first PAYING booking.

NOTIFICATIONS (templates existed, never fired):
- recordInviteFromActiveRef fires invite_joined to the inviter when their
  friend signs up.
- creditInviterOnFirstBooking fires first_booking to the inviter when a real
  credit is written. Both include data.deep_link.

INVITE TRACKING (the spine, intuitive):
- InviteTrackingScreen now leads with an earnings-at-a-glance header ($ earned
  from referrals + invited/joined/booked funnel) and a per-row +$X reward badge
  on booked friends.

FROZEN_SPEC SPEC-57 added; qa #57 added. All 60 flows green.
