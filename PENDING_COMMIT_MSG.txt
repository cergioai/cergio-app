Overview: surface "Offers to book" so the booking step is findable from the default tab

Adds an "Offers to book (N)" row at the top of the inbox Overview (the provider offers you've
received), View all → Requests, where the new "Book a time" button schedules the offer. Makes
the end-to-end barter step easy to find from the landing screen.
