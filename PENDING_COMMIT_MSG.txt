fix(profile,pdp): NAME the mutuals + connectors, consolidate PDP identity (SPEC-49g)

Reputational signals must name people, not show faceless counts, and the PDP
identity was scattered. Both fixed.

Profile (/u):
- Mutuals line now NAMES them: "1 mutual friend in common — Jane" (shared
  mutualNamesText; mutual sample fetched via getMutualConnections).
- Go-To / service-tile byline names a lead recommender even without a viewer
  mutual: "Reco'd by 2 Connectors, including Maya Rivera" (summary.leadName).
- Tightened the gap between the name and the signal block (hero pb-5->pb-2,
  signal block mt-5->mt-2).

PDP (/service):
- Consolidated the identity: TrustStream, IG reach, NAMED mutuals, and the
  bio/headline now sit together directly under the name (Tarik: "where's the
  headline / BIO", "signals all spread out").
- Killed the orphan "2 network" line — network already shows in the TrustStream
  (SocialReachLine gains includeNetwork=false; PDP shows IG only).
- Named mutuals with the viewer surfaced (getMutualConnections(ownerId)).
- Removed the separate lower "About" section (bio deduped up top); See Instagram
  folded into the identity.

Shared: reputation.jsx gains mutualNamesText + leadName naming in recoByline +
includeNetwork option on SocialReachLine.

qa.mjs #49g extended (named mutuals/connectors + PDP bio-up-top). All 75 flows
pass; all files esbuild-clean.
