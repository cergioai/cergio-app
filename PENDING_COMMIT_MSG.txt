Spotlight request: drop redundant ask line on free swaps, mutual-friends defaults to "none yet"

- Free swap: removed "Sim is offering a free X in return for a free spotlight" —
  the headline already states it. Paid keeps its line (carries the price).
- Mutual connections row now always renders once loaded; when count is 0 it
  shows "No mutual connections yet" instead of disappearing.
- (IG followers / network-on-Cergio / Connector badge still render only when
  the requester actually has those values — they were hidden only because the
  test provider had all zeros.)
