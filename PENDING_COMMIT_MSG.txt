feat(brand): elegant BETA tag + AuthScreen copy split

Tarik 2026-06-05: "add elegant beta sign next to logo, and clean up
copy look on initial sign in (too busy.. move to several lines)."

══════════════════ BETA tag ══════════════════════════════════════════

  Subtle pill rendered next to the wordmark on:

    AuthScreen.jsx — gradient green-tinted (bg-gl, border g/30, text gd)
                     sits superscript-style top-right of "CERGIO".
    SplashScreen.jsx — translucent white pill (bg-white/12, border
                     white/25) so it floats over the dark hero
                     without competing with the leaf glow.

  Same uppercase + 0.18em tracking + 6px corners on both → the BETA
  marker reads consistently across surfaces.

══════════════════ AuthScreen copy — split + breathing ═════════════════

  Before (dense):
    [Logo]
    Welcome to CERGIO
    Create your account in seconds.

  After (split + calmer):
    [Logo]
    Welcome to                          ← muted preface line
    CERGIO  [BETA]                      ← wordmark + tag, bigger
    Create your account.                ← primary action sub
    Takes seconds.                      ← supporting micro

  Sign-in variant:
    Welcome to / CERGIO BETA / Sign in to keep going. / One tap with Google.

══════════════════ Verified ══════════════════════════════════════════

  scripts/qa.mjs — 41/41 PASS
  @babel/parser JSX — AuthScreen / SplashScreen / EarningsScreen OK
