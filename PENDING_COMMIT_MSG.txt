style: right-align spotlight back link + (i) before chevron + referral test pack

═══════════════ Tarik directives 2026-06-03 ═══════════════

  (1) "move this link a little and right align ← Book a service instead"
  (2) "make the I info icon to the left of the arrow (not to the right)"
  (3) "how do i test the referral payments... share table showing
       results and triggers (notifications sent) and logins to see
       the payments per user"

═══════════════ (1) Spotlight back link ═══════════════

  HomeScreen intent strip — when intent === 'spotlight' the
  "← Book a service instead" link aligns to the right gutter
  (`justify-end pr-1`). When intent === 'find', the "Have a
  service? Spotlight it free…" CTA stays centered because that's
  the primary cross-sell direction.

═══════════════ (2) (i) icon to the LEFT of the chevron ═══════════════

  Restructured the mode toggle so it renders as
      [Free for Connectors] · (i) · ▼
  instead of
      [Free for Connectors ▼] · (i)

  The chevron is now its own sibling button — both the text and
  the chevron toggle the dropdown. (i) sits between them and
  keeps its hover tooltip + click-to-/rainmaker/apply.

═══════════════ (3) Referral payments test pack ═══════════════

  Test Referral Payments.command (sibling of Unlock and Push) —
  read-only audit, no writes. Five sections:

    1. Invite chains (who invited whom + status + reward_cents)
    2. Earnings rows by kind ('referral' direct $25,
       'referral_chain' friend-of-friend $12.50) + status
    3. Per-user totals — what each recipient will see on
       Earnings → Referrals (direct/chain counts + cents,
       cleared vs pending)
    4. Notifications log — every referral-related notification
    5. 3-hop chain visualization — inviter → friend → friend-of-friend

  Also includes a login cheat sheet:
    • Password for all seeded users: cergio-test-2026
      (re-run ./Set Seed Passwords.command if missing).
    • Step-by-step: pick a user with direct_cents > 0 from
      section 3, sign in, go to bottom-nav Earnings → Referrals,
      verify the row count + amounts match.
    • To fire a fresh payment end-to-end: sign in as a pending
      invitee, book any service, re-run the audit.

═══════════════ Files ═══════════════

  cergio-app/src/screens/HomeScreen.jsx
  Test Referral Payments.command (new launcher)

═══════════════ Verified ═══════════════

  scripts/qa.mjs — 41/41 PASS
  @babel/parser JSX on HomeScreen.jsx — OK.
