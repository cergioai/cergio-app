chore(stripe): commit public live publishable key for prod build

- Add cergio-app/.env.production holding ONLY VITE_STRIPE_PUBLISHABLE_KEY
  (pk_live_...). Publishable keys are public by design — safe to commit; it
  already ships in the browser bundle. Vite loads it at `vite build` so Vercel
  renders the live card/payment elements without a dashboard env var.
- Secret keys (sk_*, service role, API secrets) stay out of git (.env.local
  + Supabase/Vercel dashboards). Enables SPEC-58 checkout in production.
