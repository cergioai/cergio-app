Inbox key counts on both request types + counter-with-$ on free spotlight swaps

- New getInboxPartyCounts(ids) + usePartyCounts hook + formatKeyCounts: a compact
  "mutual · network · reco's · IG/TikTok" line, mutual friends always shown
  (defaults to "No mutuals" at 0).
- Spotlight inbox (ConnectorRequestsScreen InboundCard): key counts about the
  provider; Counter now shows on FREE swaps too ("Counter $").
- Free-service inbox (JobsInboxScreen "New requests near you"): key counts about
  the requester (reco's MADE). Inline $ counter already existed.
- CounterSpotlightModal: handles a $0 free base — "Name your price" (upward
  counter from free), accepts any positive amount, hides the savings row.
