Full profile mirrors the interim accept screen (SPEC-49e)

Tarik 2026-06-18 — "view full profile should show the same hierarchy as the
interim accept screen (not plumber then connector)... show the service they
spotlighted."

- ProfileSignalBlock: a Connector subject now ALWAYS leads with the Connector
  facet (reach: badge → headline → "IG · network · recos made"), with the
  service facet below — instead of "Plumber · 0 recos received" on top.
  connectorLeads = isConnector || serviceMode (serviceMode retained for the
  consumer/booking nuance, but never demotes a Connector below their service).
- PublicProfileScreen: added a "Spotlights on Cergio" section — the Connector's
  posted track record via getConnectorSpotlights, rendered as the same small
  IgPostTiles (w-[76px], 4/5, ~70%) used on the interim /inbound screen. Real
  post links only; collapses when none.
- Hierarchy now: Connector/reach → View Instagram → About (bio) → Spotlights →
  Services — matching /inbound.
- FROZEN_SPEC SPEC-49 lead-facet rule updated + SPEC-49e added; qa #49 extended
  (connector-leads + spotlight track record). All 57 qa flows green.
