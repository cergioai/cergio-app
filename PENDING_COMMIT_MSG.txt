Spotlight: earnings visibility + per-spotlight click-audit

- Earnings (motivation): getMyEarningsSummary() (earned vs pending); the Rate & post step now
  shows "You've earned $X · $Y pending" right under the 7%/$250 hook.
- Click-audit: spotlight_verified_at column + verify_spotlight(uuid) SECURITY DEFINER RPC
  (migration). The /i/{code}?s={bookingId} landing stamps that booking's spotlight "verified
  live" when the unique link is tapped (anonymous clicker → RPC). The provider's review card
  shows "Spotlight link verified ✓ — it's live and driving clicks". Soft signal; provider still
  accepts the post. This is the clickthrough proof that works NOW, before Meta Graph.
