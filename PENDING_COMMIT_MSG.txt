Profile: connector headline below badge + smaller spotlights

- Headline now renders BELOW the Connector badge and ABOVE the IG count,
  on BOTH the /inbound connector tile and the full-profile signal block
  (ProfileSignalBlock). Bio stays where it is (above the service line /
  About). Headline is separated from bio on /inbound (getInboundRequest
  already returns both); removed the duplicate name-header headline on the
  full profile. Rendered once, under the lead facet.
- "Previous spotlights" IgPostTiles shrunk ~70% (fixed ~76px tiles in a
  wrapping row instead of full-width thirds).
- Colors/fonts already follow design-spec (cream bg + DM Sans scale).
- qa #48/#49/#53 green; all changed files compile.
