Spotlight post step = Connector's unique referral link, Story-first guided flow (SPEC-47d)

Built the IG-post step around conversions + the barter→free-marketing logic:
- MarkBookingPostedModal (4★+): shows the Connector's UNIQUE link buildInviteUrl(connectorId)
  + ?s={bookingId} with a Copy button + the 7%/$250 earnings hook.
- Effortless Story-first flow: copy link → add to Story with the Link sticker → save to a
  "Spotlights" Highlight (keeps the clickable link past 24h). "Open Instagram" helper.
- Bio-link upsell (earns on every post; feed posts tag @cergio + #cergiofeed).
- JobsInbox passes connectorId so the link is the poster's own referral link.
- FROZEN_SPEC SPEC-47d. Next: per-spotlight click-audit (route logs ?s=), earnings visibility,
  and the scheduled oEmbed auto-check (activates when Meta Graph is approved).
