style: Airbnb elegance standard + Results-card pass (SPEC-49g)

Define the standard once, then apply to the highest-traffic surface.

- design-spec.md: new "Airbnb elegance standard" section — reading size is 16 not
  12 (meta is for microcopy only), real type-hierarchy jumps, whitespace over
  boxes, spacing rhythm (mt-8 / mt-4 / gap-6), numbers+names lead the signal,
  one green accent. Every screen pass follows this.
- ResultsScreen / ProviderCard: subtitle now shows the REAL provider type
  ("Hair Stylist") not the vague category ("Beauty"); bio bumped from meta(12)
  to body-sm(13) for readability. mapServiceToProvider carries
  taxonomy_provider_type.

All 75 flows pass; files esbuild-clean.
