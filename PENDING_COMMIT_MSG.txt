style(reputation): Airbnb-style TrustStream — refined segmented stat bar (SPEC-49g)

Tarik didn't like the chunky 3-card TrustStream. Restyled toward Airbnb's
understated "Guest favorite" stat bar while keeping our brand:
- ONE rounded card with hairline dividers between equal segments (was three
  separate bordered cards).
- Centered number-over-label; refined type (heading-2 numerals, not the chunky
  display-2 black). Mutuals lead and read in brand green.

Shared component, so both /inbound and the PDP update at once. All 75 flows pass;
reputation.jsx esbuild-clean.
