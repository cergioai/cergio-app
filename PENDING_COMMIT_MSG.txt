Spotlight area row: single line, Draw + On-site only (drop radius pills)

Tarik: "for the service booking connectors side, remove the radius..
just keep draw on services area (and on-site only option)... perhaps
all on one line".

Was: two-row block with a wide "Draw your service area" button on top
and an "Or quick-pick: [On-site only] [5 mi] [10 mi] [25 mi] [Anywhere]"
pill row below.

Now: ONE horizontal row with two pill-shaped buttons:
  • "Draw service area"  (or "Area drawn" + Clear when set)
  • "On-site only"       (toggle, syncs travelRadius onsite ↔ 10mi)

5/10/25mi/Anywhere quick-picks removed — providers either Draw an
explicit polygon for granular coverage OR flip On-site only for jobs
they only do at the client's location. No middle ground needed for
the spotlight intent.

Cleaner, lighter, and aligns with the same "lighten the busy
homepage" pass that moved the location chip into the search box.

QA: 41/41 pass.

Files:
  src/screens/HomeScreen.jsx
