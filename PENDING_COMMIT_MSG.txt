bundle(reco-list+nav-help+auth-tagline+bug-pic+invite-svctype)

Six related UX fixes Tarik queued 2026-06-05.

1. SplashScreen — two-line hero
   "Hi, I'm CERGIO." gets line 1 alone; value prop "I book services
   your friends actually trust." moves to a softer line 2.

2. Reco'd tile → list of recos made + edit
   - New RecoTrackingScreen at /earnings/recos.
     - Each row: recipient + "as {ServiceType}" pill (parsed from the
       persisted `[Type] blurb` prefix) + blurb body + sent-time.
     - Edit (inline textarea) patches recommendations.message.
     - Nudge (WhatsApp / SMS / Copy) rebuilds the canonical SMS the
       notify-user edge function uses, so client + server messaging
       stay in sync.
     - Inline armed-delete (no window.confirm) — matches the rule we
       already enforced on ResultsScreen + ServiceDetailProvider.
   - EarningsScreen Reco'd counts-strip tile now routes to
     /earnings/recos when count > 0 (vs the new-reco form). Tile title
     reads "tap to review, edit, or nudge" so the action is unambiguous.
   - api.js adds listMyRecommendations / updateRecommendation /
     deleteRecommendation + a split/join helper that round-trips the
     "[ServiceType] body" persisted shape cleanly.

3. Invite tracking — service-type pill + nudge clarity
   - InviteTrackingScreen joins each invite to my recommendations
     rows (digit-matched phone OR invitee_id), via the new
     getInviteServiceContexts helper, and renders a green "as Plumber"
     pill beside the Pending/Joined/Booked status pill. Hidden when
     no matching reco was sent.

4. AuthScreen — Book · Barter · Earn tagline
   The pillar line from Splash now mirrors on the sign-in surface so
   the brand pillars present at every entry point, not just splash.

5. BottomNav — Help tab (6 tabs)
   - Consumer: Search · Inbox · Earnings · Help · Activity · Profile
   - Provider: Home · Inbox · Calendar · Earnings · Help · Profile
   - Routes to /contact, the unified support/feedback inbox.
   - Life-ring icon. ACTIVE_MAP updated so /contact lights the Help tab.

6. ContactScreen — bug-report subject + screenshot upload
   - New "Bug report — something broke" subject option.
   - When subject = bug, an inline file picker reveals (PNG/JPG, 5MB cap).
   - On submit: uploads to the `contact-attachments` storage bucket
     and stamps attachment_url on the contact_messages row. If the
     bucket isn't provisioned, the message still goes through with a
     note in the body so support can ask for a re-send.

Verified: scripts/qa.mjs 41/41 PASS · JSX parses clean on all 8 files.
