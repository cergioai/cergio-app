Free-service inbox card matches the spotlight InboundCard template (reach-led sequence)

Tarik 2026-06-15: same card format both sides, but each leads with what matters —
SERVICE seeing a Connector's free-service request leads with the Connector's REACH;
Connector seeing a provider's spotlight request leads with reco's/service.

JobsInbox "New requests near you" card now mirrors the spotlight InboundCard layout:
name + "New" status pill → key counts (reach-led: IG · network · reco's made) up next to
the name → "Free service request · {timeAgo}" → ask line ("{name} is looking for a free
{service} — they'll spotlight you in return", mirror of "X is offering you a free Y") →
their message as an italic quote → location → Accept/Counter/Decline.
