Stripe go-live prep: server-canonical CC-verify via setup_intent.succeeded

stripe-webhook now handles setup_intent.succeeded — flips profiles.cc_verified_at
server-side once Stripe confirms a real chargeable card (CVV+3DS), gated on
metadata.purpose=identity_verification + cergio_user_id, idempotent on
re-delivery. Closes the long-standing TODO where the CC identity gate trusted
only the optimistic client flip (markCcVerified). Optimistic flip stays for snappy
UX; webhook is the source of truth. Header + api.js comments updated. qa 65/65.
