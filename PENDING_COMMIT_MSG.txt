Chunk B + Connector paid flag

(1) HOME: STATIC LOGO NEXT TO GREETING
Tarik: "make logo static (unless it's executing action). move up to
align next to Hi (user, tell me...)".
The pre-submit brand mark is now sized 44 + STATIC (no working prop),
anchored tight beside the "Hi Tarik, tell me what you need." line.
Pulsating bloom only fires when the search actually executes (on the
streaming-status block, which has its own larger LeafLogo with
working={true}). Hidden post-submit because the streaming-status
block owns the brand presence then.

(2) SPLASH + AUTH: HERO SIZE BUMP
SplashScreen: 88 → 120 with intensity 0.9 — first impression
unmistakably shows the new organic bud bloom.
AuthScreen: 64 → 96 with intensity 0.85 — login opens on a
confident hero-sized mark.

(3) "CERGIO IS HUMAN-POWERED AI" FOOTER: BREATHING ROOM
Tarik: "give more room (like distance above and below Claude is AI
spaces)... Cergio is AI note above the bottom nav bar".
Was bottom-[72px] (only 8px above BottomNav — cramped).
Now bottom-[92px] (28px clear) + py-2 on text itself so content
above doesn't crowd it. Matches Claude's "Claude can make mistakes"
rhythm.

(4) CONNECTOR BROWSE — FLAG PAID CONNECTORS
Tarik: "for connectors search, flag when the connectors aren't free".
Was: paid Connectors silently showed an "IG · $25/post" green pill,
easy to mistake for a "Connector means free" implication.
Now: leads each paid row with an amber "Paid Connector" pill (with a
dollar-sign glyph) BEFORE the IG/TT rate pills. Free-only Connectors
get a green "Free / swap only" pill with a shield glyph for visual
parity. User immediately sees commitment level on each row.

QA: 41/41 pass.

Files:
  src/screens/HomeScreen.jsx
  src/screens/SplashScreen.jsx
  src/screens/AuthScreen.jsx
  src/screens/BrowseConnectorsScreen.jsx
