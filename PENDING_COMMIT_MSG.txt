Dedicated connector-request screen on the live flow (SPEC-48 corrected)

Fixes the real breakdown Tarik caught: the elements we built (map, Connector
status, job details, friends-in-common) were on RequestDetailScreen
(/request/:id, the DIRECT-booking path), but the deployed "New requests near
you" flow opens /u/{requester}?reqId= (PublicProfile), whose request UI was
just a one-line Accept/Counter/Decline bar. The two paths had diverged.

New canonical screen — RequestFromConnectorScreen at /inbound/:reqId:
  - Job details (free pill, description, when_text/scheduled_at)
  - Approximate-location card (exact address only after confirm)
  - Requester block: Connector status badge + IG handle + followers +
    See Instagram link
  - Friends-in-common over the network graph (getMutualConnections)
  - Accept / Counter / Decline via respondToRequest (real actions)
  - "See full profile" link preserves the profile-drill need
  - No fake IG photo grid (gated on real media)

Wiring:
  - api.getInboundRequest(reqId) selects the full requests row (timing,
    location, free flag, budget) + requester profile (IG, cc_verified_at).
  - App.jsx route /inbound/:reqId.
  - JobsInboxScreen "New requests near you" card now opens /inbound/:reqId
    instead of the bare profile ?reqId path.

SPEC-48 retargeted to the canonical connector-request screen + qa #48 now
asserts the inbox routes to /inbound/:reqId and the screen carries every
element. RequestDetailScreen stays as the direct-booking detail screen.
