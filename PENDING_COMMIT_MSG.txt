Post-gate fires earlier + inbox dot covers spotlight requests

Tarik 2026-06-18.

- SPEC-47i rev: the forced rate/post lock now fires once the barter service
  has HAPPENED — provider marked complete OR the scheduled time has passed —
  not only after the provider remembers to mark complete. getOutstandingFreeBarter
  returns a serviceHappened flag; BarterPostGate blocks on it; copy adapts
  ("Your {service} is done" when only the time has passed). Future-dated
  barters still don't block.
- Inbox dot now covers SPOTLIGHT requests too: a new inbound spotlight ask, or
  the other party responding so it's your turn (inbound countered-by-provider;
  outbound accepted / countered-by-connector). Wired listMyInbound/Outbound
  SpotlightRequests into useInboxUnread.
- FROZEN_SPEC SPEC-47i updated; qa #47i + #50 updated. All 58 flows green.
