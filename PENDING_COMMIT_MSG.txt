test(ontology): full-catalogue precision gate — proves "Ferrari" coverage (SPEC-68)

The catalogue was already effectively complete; this proves it and locks a
regression gate. NO resolver/taxonomy change → no edge-function redeploy needed;
this is tooling only (live matching behavior is unchanged).

scripts/audit-ontology-coverage.ts (new): runs EVERY real search term in the
taxonomy — 7,285 terms across 932 offerings — through resolveQuery and classifies
each: correct / ambiguous-OK (cross-listed across types) / →Claude / TRUE
confident-wrong. Excludes generic "Service Provider"-intended terms (the resolver
deliberately emits a specific type, SPEC-67c) and cross-listed ambiguous terms.

Result (2026-06-26):
  correct (≥.60, right type):  5,765  (79%)
  ambiguous-OK (cross-listed): 1,467  (20%)
  →Claude (miss):                  0
  TRUE confident-wrong:           53  (0.73%)

All 53 are intentional parent/synonym BRIDGES — the resolver notifies the broader,
populated provider type instead of a hyper-niche child with no registered
providers (e.g. "EV charger"→Electrician, "piano moving"→Mover, "duct cleaning"→
HVAC, "balayage"→Hair Stylist, "lash extensions"→Esthetician). Several are MORE
correct than the taxonomy's label (e.g. "photographer"→Photographer, which the
taxonomy filed under "Gender Reveal Coordinator"). Zero harmful "caterer→cat".

The audit is a regression gate (budget 60; exits 1 if confident-wrong climbs,
catching a NEW harmful match without failing on the known-good 53). Run via the
new top-folder "Audit Ontology Coverage.command" (add --wrong to list all).
The labeled eval (scripts/eval-ontology.ts) stays green: 109/109, 0 confident-wrong.
