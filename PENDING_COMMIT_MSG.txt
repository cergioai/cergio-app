Full profile lead block = exact interim /inbound identity block (SPEC-49e)

Tarik 2026-06-18 — "follow this exact structure on the full profile (incl.
See Instagram by the connector stats)."

ProfileSignalBlock rebuilt to replicate the interim accept screen's identity
block 1:1 for a Connector subject:
  Connector  →  headline  →  "319 IG followers"  →  "5 network on Cergio ·
  5 reco's made"  →  See Instagram  →  bio  →  "Plumber (0 recos received)"
  →  "You have no mutual friends with T yet."
- Reach + strength are now separate lines (not the combined formatKeyCounts
  one-liner); bio + See Instagram are folded INTO the block.
- PublicProfileScreen drops the standalone "View Instagram" link and the
  separate "About" section (now inside the block); passes bio + igHandle +
  name down.
- Data source unchanged (getInboxPartyCounts); no fake data.
- FROZEN_SPEC SPEC-49e updated; qa #49 extended to lock the exact structure.
  All 57 qa flows green.
