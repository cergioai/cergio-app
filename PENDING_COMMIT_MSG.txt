Credit-card identity gate on POST + test-account bypass (SPEC-59)

Tarik 2026-06-19 — no one can publish a spotlight post without a verified card;
test accounts bypass.

- Test-account bypass centralized in getMyCcStatus: returns a synthetic
  cc_verified_at (+ cc_bypass) for IDENTITY_BYPASS_EMAILS (t@cergio.ai,
  info@cergio.ai). Single source of truth — every gate that reads it
  (post / request / listing / photos) now lets the test accounts through
  without a real card.
- POST gate: MarkBookingPostedModal (barter rate+post — gates only the actual
  publish; held <4★ ratings and paid no-post recommendations still pass) and
  MarkPostedModal (paid spotlight) now require cc-verified; if not, they open
  CcGateModal (reason 'post', Stripe SetupIntent — no charge) and proceed on
  success.
- New 'post' reason copy in CcGateModal.
- FROZEN_SPEC SPEC-59 added; qa #59 added. All 62 flows green.

NOTE: for REAL (non-bypass) users the card check needs the create-setup-intent
edge function deployed + the Stripe secret set; that infra already exists in the
repo (supabase/functions/create-setup-intent).
