/inbound: differentiate reco's made vs received + name services; fix dup date

- Connector strength now distinguishes "{N} reco's made" (recommendations the
  Connector gave) from "{N} received" (recommendations on their own services),
  and lists service names ("Services: Plumber, Dog Walker"). getPublicProfileStats
  now returns recosReceived + serviceNames (recos-received = recommendations on
  services they own).
- Fixed the personalized message doubling the date ("...today today") when the
  raw request text already contains the timing.
