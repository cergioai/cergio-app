Rename GOAT → Connector across user-visible UI + seed

Tarik: "change goat to connector". Consistent product terminology
across all surfaces. The internal Connector tier was being labeled
"GOAT" in a few spots inherited from the original mockup vocabulary
— that's now harmonized.

UI copy:
  ServiceDetailScreen.jsx
    "Free for GOATs"  →  "Free for Connectors"  (x2 — pill + offering)

  ActivityScreen.jsx
    "GOATs have shared their go-to services on Cergio"
      → "Connectors have shared their go-to services on Cergio"
    "#cergiogoats"  →  "#cergioconnectors"
    "Shared by {name}, GOAT"  →  "Shared by {name}, Connector"

Seed data (Seed Miami Services.command):
    Profile "Sofia GoatClean"               →  "Sofia Cleaner"
    Service "Sofia's Free-for-GOATs Clean"  →  "Sofia's Free-for-Connectors Clean"
    Offering "Deep Clean (GOAT perk)"       →  "Deep Clean (Connector perk)"
    Rec "Sofia is free for GOATs..."        →  "Sofia is free for Connectors..."
    Rec "GOAT-perk listing..."              →  "Connector-perk listing..."
    Cleanup DELETE clause widened to drop BOTH old + new titles so the
    re-run is idempotent on already-seeded DBs.

Internal symbols (listGoatShares helper, goatShares state, GoatShareCard
component, lib/api.js comments, LogoLabScreen mascot-color comment)
NOT renamed — they're code-side identifiers + design notes that
reference the orange goat mascot icon (which is the actual brand
mark, not the Connector tier). Keeping those decouples vocabulary
from variable names.

QA: 41/41 pass.

Files:
  src/screens/ServiceDetailScreen.jsx
  src/screens/ActivityScreen.jsx
  + Seed Miami Services.command updated on disk (re-run after push)
