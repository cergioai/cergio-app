spec: add FROZEN_SPEC.md + qa.mjs guards #42–#45

Every confirmed behavior is now a blocking QA test. If it breaks,
the push fails. No regressions can reach main silently.

FROZEN_SPEC.md — new file, permanent spec document:
  SPEC-42: no barter pill on results waiting state
  SPEC-43: invite contacts from network table only, no pre-selection
  SPEC-44: geocoder error clears when Nominatim rescues
  SPEC-45: free ($0) spotlight skips Pay step + paid_at gate
  SPEC-12: no mock data on signed-in screens (pre-existing)
  SPEC-CQ1/CQ2: influencer 10k–50k follower band (crawler-side)

qa.mjs — added tests #42, #43, #44, #45 enforcing the above.
All 4 new tests pass. Full suite unaffected.

Process: confirmed chat behavior → FROZEN_SPEC.md entry → qa.mjs test → push.
Nothing confirmed by Tarik regresses silently again.
