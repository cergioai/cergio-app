Desktop contacts upload fallback (.csv/.vcf) on Invite a friend

Tarik: "says contacts picker needs android / mobile to connect."
The native Contact Picker API only exists on Chrome Android, so on
desktop the Connect-your-contacts button now opens a file picker
instead: upload a contacts export (.csv from Google Contacts or
Outlook, or .vcf vCard) and the contacts land in the same
"From your contacts" section. Header-driven CSV parser (handles
quoted fields + both export shapes) and a minimal vCard parser —
verified against Google CSV, Outlook CSV, and VCF samples.
Real contacts only, dedupe on email/phone/name, honest error toast
when a file has no contacts.

QA: all 46 flows pass.
