feat(ontology): depth supplement batch 2 + eval gate (the standard)

- SUPPLEMENT batch 2: locksmith, pest control, window/carpet cleaning, mobile
  mechanic, tailor/seamstress, bartender, arborist, wedding/event planner,
  interior designer, tax prep, bookkeeping, web dev, graphic design (EN + ES).
  All map to verified-existing provider types.
- THE STANDARD: scripts/eval-ontology.ts — labeled coverage+precision gate.
  Hard rule: FAILS on any phrase resolving >=0.60 to the WRONG type (zero
  confident-wrong). Tracks local-coverage %. Launcher: "Eval Ontology.command".
- Current eval: 89 cases, 100% correct local, 0 confident-wrong. 74 qa flows pass.
- Pattern is now repeatable: add a batch to SUPPLEMENT → run the eval gate →
  ship. Ready to run the full catalog (the remaining ~580 thin types) in batches.
