"Network on Cergio" = composite (invites + recos made/received + follows)

The Cergio-network count was just follows (0 for most), so it never showed.
Redefined networkCount as registered invites + reco's made + reco's received +
follows, and relabeled it "{N} network on Cergio" so it surfaces meaningfully.
