Profile brand pass v1 (re-skin, layout unchanged)

Tarik 2026-06-18 — match the brand across both profiles.

- PublicProfileScreen: premium brand hero — soft mint→cream gradient band
  (design-spec `gl` token) behind the avatar + name; avatar enlarged 72→84
  with a white ring + shadow. Layout/sections unchanged.
- ProfileScreen (account hub): hero avatar now uses the brand green gradient
  (g→gd) with a white ring, instead of a flat grey circle.
- Fixed: account-hub "My earnings" drawer pill was hardcoded "$0 USD" — now
  shows the real lifetime total (matches the card above it).

All 60 qa flows green.
