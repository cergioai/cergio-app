SPEC-66: SMS outreach channel (gated, compliant) per Tarik override

outreach-send now also texts leads we have a phone for but no email (one
channel per lead — email preferred, SMS fallback) via Twilio. Gated by
OUTREACH_SMS_ENABLED (default false). Every text: identity + "Reply STOP to
opt out" (Twilio Messaging Service auto-honors STOP), suppression-checked,
send-once, E.164-normalized. Returns sms{enabled,configured,sent} for the
dashboard. WhatsApp cold still excluded (Meta policy). Updated FROZEN_SPEC
SPEC-65/66 + qa #65 -> 69 flows pass.
HARD PREREQS to deliver: Twilio creds set + US A2P 10DLC brand/campaign
registered (carriers block unregistered). TCPA risk accepted by operator;
counsel recommended.
