style(reputation,pdp): remove TrustStream boxes → inline text; bigger review type (SPEC-49g)

Airbnb-clean elegance pass on the reputational + review surfaces.

- TrustStream: removed the boxes entirely — now ONE inline line of text, bold
  number + muted label, middot-separated ("1 mutual with you · 3 on Cergio · 3
  recos made"), mutuals in brand green. Shared, so /inbound + PDP both update.
- PDP "What people say": readable Airbnb sizing — name at body, quote at body-lg
  leading-relaxed (was tiny meta italic), avatar 36→40, generous 24px row gaps,
  no card borders. Header bumped to heading-1.

All 75 flows pass; files esbuild-clean.
