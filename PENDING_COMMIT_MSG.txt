fix(outreach): target the real services bucket + approved-only gate; truthful copy

Data reset GO (per DATA QUALITY STANDARD & RESET PLAN):
- Repoint all outreach (email + WhatsApp gen + SMS) from leads_localbiz
  (brick-and-mortar, Phase 2) to **leads_services** (the real mobile/independent
  provider bucket). leads_localbiz is no longer contacted.
- Outreach now sends ONLY outreach_status='queued' (gate-APPROVED). Raw 'new'
  leads are never contacted — so ungraded/garbage can't be blasted, and the bad
  creator bucket (leads_influencers, all 'new') is inert until rebuilt + graded.
- Messaging integrity (per feedback_outreach_messaging_principles): never
  misrepresent — creators invited to a "founding group → first access as we
  onboard providers" (no claim services exist yet); "independent & mobile
  providers", not "local businesses" (Phase 2); signed "Tarik, Cergio".
- /free landing copy aligned to the same.

Companion (not in repo): "zzz RUN ME - Grade & Approve Services.command" (the
gate: approves reachable mobile/home providers → queued, quarantines off-values/
food/retail/storefront → do_not_contact) and "CREATOR RE-SOURCE BRIEF" (fix the
creator source: social discovery of real individuals, not map data).

QA: all 76 flows pass. Deploy edge functions after push so the repoint goes live.
