Fix: move `sent` useState above tabCounts (TDZ crash — "Cannot access before initialization")

tabCounts referenced the `sent` state before its declaration, white-screening /inbox.
Declared `sent` alongside `myJobs` (above the counts); kept the eager-load effect.
