Search: force provider_type at the consumer (ResultsScreen render)

DEFINITIVE FIX for the React-HMR-doesn't-refresh-hooks pattern.

Previous attempts:
  - Override in applyParseResult (useChat:440)  →  hook stays stale
  - Force at merged-build site  (useChat:520)    →  hook stays stale
Both produce correct code in the FILE but React keeps the OLD
closure on the live mounted component until full page reload.

This fix runs the local-taxonomy lookup AT THE CONSUMER — every
render of ResultsScreen. Pure derivation from userQuery; no
dependency on useChat hook freshness. HMR of a screen file is
reliable; HMR of a hook is not.

  const localResolvedPT = resolveProviderTypeLocal(userQuery);
  const provider_type = localResolvedPT || rawProviderType;

Everything downstream (listServices call, title text, safe filter,
pills) now derives from the deterministic local value.

Also fixed the new title format that was previously pluralizing
the wrong base ("Toilet replacements"): since provider_type is
now corrected at the source of the title computation, the plural
helper sees "Plumber" and produces "Plumbers".

Expected: "Looking for plumbers to unclog your toilet" → J. Chen.

Files: src/screens/ResultsScreen.jsx
qa.mjs: pass (14/14)
