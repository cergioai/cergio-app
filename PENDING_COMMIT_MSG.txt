feat(ux): seed dense fixtures + empty-state nudges + splash trim

Tarik 2026-06-04 directive: "populate with good data to seed and do
rigorous UX testing on each page, flow text to spot anything that
needs to be optimized... especially barter referrals, direct
bookings... share recommendations (but implement without waiting)."

══════════════════ Seed ═══════════════════════════════════════════════

  Ran Seed Dense Network.command against the live Supabase:

    Profiles: 20 (6 Connectors + 6 Providers + 8 Users)
    Network edges: 50+ follows, 3 chains depth ≥2
    Invites: 8 rows, tree depth 3
    Services: 6 (one per provider) with 2 offerings each
    Bookings: 4 paid (referral fixtures) + 3 free-for-Connector
    Spotlights: 9 — full status coverage
    Earnings: 3 spotlight payouts in distinct states

  Idempotent `den-` UUIDs — doesn't touch the existing Miami seed
  (Connie / Sofia / Henry…).

══════════════════ UX bundle v1 — ship without waiting ════════════════

  1.  SPLASH copy trim.

      Before: "I'll negotiate AND book services your friends actually
              trust." — three claims stacked into one sentence (dense).
      After:  "I book services your friends actually trust."

      The Book / Barter / Earn pillars below already carry the feature
      breakdown. Hero now has a single load-bearing claim.

  2.  EARNINGS counts strip — empty-state nudge.

      The five-tile strip (Invited / Joined / Booked / Reco'd / Services)
      used to render "0 0 0 0 0" when the user is brand new — hollow.
      When the sum is 0 across all five, the strip now collapses into
      a single gradient CTA card:

        "Invite your first friend — earn $250 when they book.
         Plus $12.50 chain bonus when their friends book too. Send invite →"

      Tap routes to /invite/friends-popup. The full 5-tile strip
      returns the moment any count > 0.

  3.  ACTIVITY feed — Follow-Connectors empty state.

      The unified social feed silently disappeared when the viewer
      followed nobody yet — felt like the screen was broken. New empty
      state when `followedIds.size === 0`:

        "Follow Connectors to see their picks here.
         Connectors are the locals who spotlight services worth
         booking. Browse Connectors →"

      Routes to /connectors (or /auth for guests). Once the viewer
      follows ≥1 actor, the real feed renders normally.

  4.  PROFILE "By the numbers" — self-view first-action nudge.

      Hollow profiles (own + zero stats) now show a small gradient
      card pointing to the first invite. Other people's empty profiles
      still hide cleanly — no point teasing strangers.

        "Just getting started.
         Invite your first friend to start building your network.
         Send invite →"

══════════════════ Verified ══════════════════════════════════════════

  scripts/qa.mjs — 41/41 PASS
  @babel/parser JSX — Splash / Earnings / Activity / PublicProfile OK

══════════════════ Notes on what didn't ship in this bundle ═══════════

  • Recommendation flow one-at-a-time rework — structural, deferred.
  • Booking-flow waiting-state copy ("Sent to Connectors near you")
    — touches ResultsScreen which is 1217 lines, defer.
  • Activity-tab red-dot for unread feed events — needs a read/unread
    table that doesn't exist yet.
  • Universal copy tightening on dense paragraphs (Earnings explainer
    Scenes 1-3, About mission). Touch points are spread out.
  • Guest-mode BottomNav teaser (Earnings/Inbox/Activity → "Sign in to
    track") — touches BottomNav + each screen's signed-out state. Defer.

  Per "i'll reverse from the list if necessary" — flag anything in this
  bundle that should be rolled back.
