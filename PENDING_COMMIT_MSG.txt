Frame 3 tile reorder + search form date/time picker

- Frame 3 connector tile restructured (Tarik): identity first — avatar · name ·
  Connector badge · bio · services offered (+ reco's received) — then the IG box
  (handle · followers/reach · See Instagram), then mutual friends BELOW the IG
  box, then See full profile. Surfaces service reputation above connector reach.
- Search form (IntakeFormScreen): replaced the free-text "When" with a real
  date + time picker. "I'm flexible on the exact time" hides the time field
  (the provider picks it on the request screen). A specific start time is now
  required unless flexible. (Budget already present = the paid signal.)
