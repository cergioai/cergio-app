Close standing items: requester Q&A reply, quarantine ?reqId bar, de-mock /job + /complete

1. Requester reply surface — the connector now answers pre-booking questions in
   their Inbox (Requests tab → "Questions to answer", reply inline via
   replyRequestQuestion). New api listMyRequestQuestions; badge + empty-state
   account for open questions. Closes the Q&A loop.
2. Quarantined the orphaned /u/:id?reqId= sticky Accept/Counter/Decline bar in
   PublicProfile (superseded by /inbound/:reqId, SPEC-48) — reqCtx is never
   hydrated, so the dead bar can't render.
3. De-mocked the post-accept screens (SPEC-12):
   - /job (JobDetailsScreen) rebuilt on the REAL booking via getBooking — real
     client, service, time, location, details (no more Jennifer/David/Broadway).
   - /complete (ServiceCompleteScreen) reads the real client + reach from
     RateConfirm's router state — no fabricated "Lydia · 23,735 followers".
