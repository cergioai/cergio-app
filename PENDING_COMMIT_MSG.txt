design: crossed-out "FREE" price, energetic v7 logo, dedupe Pick tag, fix Reco Connect

PDP — free offering shows $X crossed out + big FREE
  Tarik: "for free for connectors... show the official price but
  crossed out and show free instead".
  Free offering card now renders:
    [ ~~$160~~  FREE  🛡 for Connectors ]
  Reference price priority:
    1. Highest-priced sibling paid offering on the SAME service
       (captures "the same provider's normal rate").
    2. Category-based fallback table (House Cleaner $160, Personal
       Driver $80, Babysitter $75, etc.) when this service only
       lists the free perk.
  Layout is left-anchored (justify-start + self-start) so the strike-
  through price sits under the offering name and lines up clean.

Logo motion v7 — ENERGETIC contract + expand (LeafLogo.jsx + index.css)
  Tarik: "make animation contract and expand in fun animation.. it's
  boring now.. make energetic".
  v6 was calm/fluid drift — leaves never got below scale 0.94 and
  rotation stayed within ±2.5°. v7 keeps the layered architecture
  (breath + drift + shimmer + slow spin) but pumps the amplitudes
  AND tightens the tempos so the silhouette is in CONSTANT PUMP:
    • Breath: scale 0.78 ↔ 1.18 (was 0.94 ↔ 1.06) — true contract
      + expand
    • Drift rotation: ±8° (was ±2.5°) — leaves nod with attitude
    • Drift translate: ±2px (was ±1px)
    • Per-leaf breath tempo 2.1–3.7s (was 3.7–5.7s)
    • Per-leaf drift tempo 3.1–4.3s (was 4.7–6.7s)
    • Whole-mark spin 12s (was 24s)
    • Heart counter-spin 18s (was 38s)
    • Easing cubic-bezier(.42,0,.58,1) — symmetric ease IN-OUT for a
      confident PUMP feel (was .5,0,.5,1 for a soft drift)
  Result reads as a playful, breathing flower that's clearly "doing
  something", not gently floating. Still all coprime tempos so no
  cycle repeats.

ProviderCard — duplicate "Cergio Pick" tag removed
  Tarik: "remove one of the cergio's pick's tags... its duplicated".
  Cards rendered the green pill twice when `pick` was true (photo
  top-right overlay + pill above the name). Dropped the in-content
  pill; the photo overlay reads as the canonical Featured badge.

Reco form — Connect button now actually connects
  Tarik: "connect on reco form still not connecting to contacts.. (seed
  with data...)... so test reco's invites.. (test rigourously first)".
  The Connect button used to toast "Contact picker isn't supported on
  this browser" on desktop and bail. Now when navigator.contacts is
  unavailable, the button promotes the seeded pool (real DB profiles
  + deterministic synthesized phone/email from listInvitableProfiles)
  into `synced` — autosuggest is primed, the Connect card disappears,
  and the user gets confirmation toast ("N sample contacts loaded —
  start typing a name"). End-to-end testable from any browser.

Verified: qa.mjs 41/41 · JSX syntax check on every touched file: green.
