fix(services): provider service page back-button dead route + UX note

- ServiceDetailProviderScreen "Back to my services" pointed at /profile/services,
  which is NOT a route → catch-all bounced it to home. Now → /services/manage.
- (Context: tapping a service in Manage correctly opens its manage page; the
  separate "+ List another service" button is the new-listing entry. The page
  loads fine for the owner — this was a stray dead route, not a load failure.)
