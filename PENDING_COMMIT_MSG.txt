BrowseConnectors: dedupe the spotlight-request buttons (two same-titled listings showed twice)

The broadcast→"we'll notify you / Cancel request" waiting state already mirrors the consumer
request flow; the entry just rendered one button per listing, so two listings with the same
title ("Personal Chef in Miami…") produced two identical buttons. Dedupe by title.
