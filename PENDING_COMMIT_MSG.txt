Drive /inbox BOOKINGS cards to the new card template (off the old format)

Tarik 2026-06-15: /inbox still showed the old request format; the updated design is the
spotlight/"New requests near you" template. Booking cards now match it exactly:
name + "Needs response" pill → reach-led counts (IG · network · reco's made) → "Booking/
Free service request · {date}" → ask line ("{name} wants to book your {service}" / "is
looking for a free {service} — they'll spotlight you in return") → their note as an italic
quote → appointment. Tap still opens /request/:id. One card format across the inbox.
