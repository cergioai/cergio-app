feat(soft-launch): public free-barter landing + founding-cohort copy (SPEC-70)

- New PUBLIC landing at /free (FreeBarterLandingScreen): two browsable tabs —
  "Free services" (listed services with a $0 offering) and "Creators for
  spotlights" (profiles with an IG/TikTok handle, biggest reach first). Real DB
  rows only; honest empty states; rows tap into the existing PDP (/service/:id)
  and profile (/u/:id) flows; footer CTA -> /auth?src=soft_launch. This is where
  opted-in founding members become visible + reachable, closing the loop:
  outreach -> opt-in -> claim/list -> discoverable here -> direct request.
- Outreach email copy: added founding-cohort / insider-perks framing on both
  the business and creator emails (CTA -> "Claim a free founding spot").

Frontend deploys via Vercel on push. The email-copy change is in outreach-send
(edge fn) — needs a redeploy (zzz RUN ME - Deploy.command) to show in live emails.
QA: all 76 flows pass.
