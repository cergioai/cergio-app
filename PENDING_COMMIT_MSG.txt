Email senders → notify@cergio.ai (domain verified in Resend)

cergio.ai verified in Resend (2026-06-12): DKIM TXT resend._domainkey,
SPF TXT + MX on the `send` subdomain added at GoDaddy (root MX left
untouched — Google Workspace mail unaffected). All four edge functions
(notify-provider, notify-user, notify-spotlight, notify-request) now
send from Cergio <notify@cergio.ai> instead of the Resend sandbox
sender, which only ever delivered to the account owner. This was the
root cause of "no email at t@/info@cergio.ai".

Also fixed at GoDaddy while in there: the truncated TikTok
developers-site-verification TXT now carries the full token.
