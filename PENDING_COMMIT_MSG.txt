Sprout v2 + dialed motion + $12.5K Home pill + friend-of-friend 5%

LEAFLOGO — SPROUT V2 (user picked option B → "infinitely more
interesting … subtle, captivating")

Replaced single lobed-leaf with a botanical sprout: two asymmetric
serrated leaves on a slightly S-curved stem rising from three soft
brown soil-mark dashes, with a subtle dew drop on the top leaf.

Multi-motion: four overlapping micro-cycles with different periods
(8s stem / 7s top / 6s bot / 5s dew) — never resolves into a
rhythm. Amplitudes DELIBERATELY tiny per user follow-up brief:
"barely visible … elegant … so users don't get annoyed."
Max sway 2° at full intensity (was 6° in the first pass). Strength
+ community are inferred from shape and color, not from movement.

The intensity-prop API is unchanged — Home / Splash / Results /
Auth / Intake all auto-inherit the new look + dialed motion.

$12.5K HOME PILL — exposes the compounding math

User feedback: "expose the earn 12.5K when you invite 50 users …
without being tacky intrusive."

Single compact line below the search box in find mode:
  "50 friends → $12,500   see how"
The "see how" is a small green underline link to /rainmaker/apply.
Looks like a footnote, not a sales card.

FRIEND-OF-FRIEND BONUS — 5% second-tier added everywhere

User: "can we add a friend of friend incentive … they take 5% of
the 250." Locked in REWARDS:
  friendOfFriendPercent = 5
  friendOfFriendBonus   = 12.5

Surfaced everywhere it's relevant:
 • REWARD_COPY: friendOfFriendOneLine / friendOfFriendShort
 • Connector explainer popup (ProfileScreen): bullet in the reward stack
 • EarningsScreen: kicker line under mode-tailored copy
 • RainmakerApplyScreen: bullet in BOTH User + Connector columns

PROFILE DECLUTTER

Earlier commit re-grouped Profile into Account / Earn $250+ / About,
with provider-only sections hidden for consumers. This commit
extends that with the new Connector explainer popup that now also
mentions friend-of-friend.

VERIFIED  qa.mjs: 30/30 pass

Files:
  src/components/ui/LeafLogo.jsx
  src/index.css
  src/lib/rewards.js
  src/screens/HomeScreen.jsx
  src/screens/ProfileScreen.jsx
  src/screens/EarningsScreen.jsx
  src/screens/RainmakerApplyScreen.jsx
  scripts/qa.mjs
