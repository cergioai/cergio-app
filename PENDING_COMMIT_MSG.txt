SPEC-48b: booking detail to frame-3 parity + new-card-only inbox + quarantine the FALLBACK mock

Why it looked regressed: nothing reverted — the booking detail /request/:id only got the
first SPEC-48 pass; this session's newer polish (key counts, Connector badge) landed only on
/inbound & /spotlight. This brings /request/:id (free AND paid) to full parity and freezes it.

- RequestDetailScreen (/request/:id): + Connector badge + key-counts line (network · reco's
  made · IG · TikTok via shared usePartyCounts/formatKeyCounts; mutuals stay in the dedicated
  friends-in-common block). Same elements as /inbound, for free and paid bookings.
- QUARANTINE: the demo FALLBACK mock (hardcoded fake requester) is unplugged — a missing/
  invalid booking now shows a clean "no longer available" state, never fake data (SPEC-12).
- JobsInbox: booking cards now carry the SAME key-counts line as the connector free-request
  cards — one card design for both request types. One shared count source (usePartyCounts).
- FROZEN_SPEC SPEC-48b + qa #48 extended to guard parity, the shared hook, and the quarantine.
