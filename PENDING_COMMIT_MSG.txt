IG post tile on the feed + Previous-spotlights track record (SPEC-47f)

- IgPostTile: honest tappable IG-branded tile (gradient + glyph + "View", links to post_url) —
  no fabricated photo until Meta media access (SPEC-12); swap to real <img> then.
- Activity feed barter item now shows the spotlight as an IgPostTile (replaces the text link).
- getConnectorSpotlights(connectorId): a Connector's prior confirmed barter posts. The frame-3
  "Previous spotlights on Cergio" section is now populated from it (was a reserved placeholder),
  giving providers the Connector's posting track record before they accept.
- FROZEN_SPEC SPEC-47f.
