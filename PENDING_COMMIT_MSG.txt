Fix duplicate service names ("Plumber, Plumber")

getPublicProfileStats now de-duplicates services by display name, summing each
name's reco count across duplicate listings — so the tile shows "Plumber (N)"
once instead of repeating it.
