feat(taxonomy): out-of-scope categories denylist + CRAWLER_BRIEF.md

Tarik 2026-06-03:
  (1) "yes except always exclude the following categories from both
       services and physical places... there're outside business
       focus... Food + drink — restaurants, breweries, wine bars,
       food trucks... DJ's... Massage therapists, dance studios..."
  (2) "also export instructions related to this crawler to share
       with an agent building directories under a different project
       to run from there... to keep this for platform dev"

═══════════════ (1) Out-of-scope denylist ═══════════════

  src/data/providerTypes.js:
    • OUT_OF_SCOPE_PROVIDER_TYPES set added (exported).
    • isOutOfScopeProviderType(value) helper (case-insensitive).
    • Removed from PROVIDER_TYPES (kept as commented placeholders):
        - DJ
        - Dance Floor Supplier
        - Dance Instructor
        - Kids Dance Instructor
        - Latin Dance Instructor
        - Food Truck Operator
        - Massage Therapist
        - Mobile Massage Therapist
        - Prenatal Massage Therapist
        - Sports Massage Therapist
    • Restaurant / Cafe / Brewery / Winery / Wine Bar / Pub /
      Cocktail Bar / Sports Bar / Distillery / Food Cart added
      to the denylist defensively (none were in the original
      taxonomy but they're locked out for the crawler import path).

  src/lib/serviceTaxonomy.js:
    • Dropped the 'massage' / 'masseuse' → 'Massage Therapist'
      mappings.
    • resolveProviderTypeLocal now short-circuits to null when the
      user text contains any out-of-scope keyword (massage, dance,
      DJ, food/drink venues). Belt-and-suspenders against future
      taxonomy.json re-adds.

  scripts/qa.mjs:
    • Dropped the 'massage therapist' → 'Massage Therapist' search
      case from invariant #29 — that route is now intentionally null.

═══════════════ (2) CRAWLER_BRIEF.md ═══════════════

  New doc at Cergio Claude/CRAWLER_BRIEF.md — self-contained
  spec for the SEPARATE project / agent that builds the
  local-business + influencer directories.

  Sections:
    1. Output schema (local_businesses.csv + influencers.csv)
    2. Exclusion list (verbatim from OUT_OF_SCOPE_PROVIDER_TYPES)
    3. Inclusion rules for local-biz directory — brick-and-mortar
       only, plus the high-ad-spend verticals to include
       (accountants, lawyers, florists, dental, HVAC, funeral
       homes, hearing aids, bail bonds, etc.)
    4. Canonical provider_type strings — pull from
       src/data/providerTypes.js
    5. DO NOT crawl Craigslist — anti-bot + 3taps/CFAA precedent +
       ToS prohibition. Use Google Business Profile / Yelp Fusion /
       Yellow Pages / BBB / OSM / OpenCorporates instead.
    6. Output cadence + handoff (weekly full + daily delta)
    7. Coordination — PRs to this brief, NOT to cergio-app/
    8. Canonical files in the platform repo to mirror

═══════════════ Files ═══════════════

  cergio-app/src/data/providerTypes.js
  cergio-app/src/lib/serviceTaxonomy.js
  cergio-app/scripts/qa.mjs
  CRAWLER_BRIEF.md (new, lives at root next to MARKETPLACE_SPEC.md mirror)

═══════════════ Verified ═══════════════

  scripts/qa.mjs — 41/41 PASS
  @babel/parser JSX on providerTypes.js + serviceTaxonomy.js — OK
