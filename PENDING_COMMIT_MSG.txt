fix(notify): chef requests reach chefs — kill stale/null provider_type (spine)

Root cause of "Marco/info@ didn't get Maya's chef request": the request was
fanned out as provider_type "Immigration Consultant". Diagnosis (live edge fn +
DB): the chef providers were fine (info@ + Marco both Personal Chef, listed,
Miami). The break was the request's provider_type:
  1. When a turn went to Claude (sub-0.60), chat-parse returned NO provider_type
     (Claude gives a service name, never mapped back to a type) → client kept a
     STALE provider_type from an earlier turn.
  2. Bare "chef" phrases weren't resolving locally.

Fixes:
- resolver: SUPPLEMENT chef terms (chef / need a chef / dinner party chef /
  cocinero …) → Personal Chef; single-word substring guard 5→4 so "chef" (4)
  resolves inside phrases. "need a chef tonight" & "chef who can cook ecuadorian
  food" now → Personal Chef locally.
- chat-parse: the Claude path now RE-RESOLVES Claude's answer so
  _resolver.provider_type is ALWAYS set (never null → never stale).
- HomeScreen: RE-RESOLVE the service fresh at submit (authoritative over chat
  state) + notifySafe when a type is present, so a stale type can never fan out.
- Eval gate: 89/89 correct, 0 confident-wrong. 74 qa flows pass.
