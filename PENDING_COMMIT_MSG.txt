PDP polish + free-form request + Go-Tos→Recommendations + no-duplicate-listings (SPEC-60)

Tarik 2026-06-19 — design audit + bug grind.

- DUPLICATE-SERVICES BUG: list flow double-fired createService (initial-verified
  + post-gate + strict mode) → 2 identical rows. Fixed both layers:
  ServiceListSetupScreen guards the persist effect with a one-shot submittedRef;
  createService de-dupes (returns an existing same-title listing by this owner
  from the last 2 min instead of inserting again).
- PDP offerings: lighter/thinner selection (border-g/70 + faint mint fill,
  border-line unselected) — no heavy 1.5px ring; matches the attached design.
- PDP "Submit a request for a custom quote" now opens the homepage FREE-FORM
  (/home with a prefill), not the structured quote sheet.
- Terminology: profile "Go-Tos" → "Recommendations" ({name}'s Recommendations).
  (GOAT→Connector / Romio→Cergio already in code; Activity copy already says
  Connector.)
- FROZEN_SPEC SPEC-60 added; qa #60 added. All 63 flows green.
