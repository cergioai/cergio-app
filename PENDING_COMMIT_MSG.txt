Spotlight audit fixes + mock-data purge + 20-user simulated test (12/12 green)

MOCK DATA REMOVED (no fake data on real screens):
- CalendarScreen: dropped CALENDAR_BOOKINGS mock for signed-out viewers
- ManageServicesScreen: signed-out gets real empty state, not Jamie/John/Steve
- ServiceDetailProviderScreen: non-UUID ids -> not-found state, never a mock listing
- ProfileScreen: dropped PROFILE mock fallback name
- RainmakerRequestScreen: rewritten generic explainer (was fake "Reyna"/6,974
  followers on a LIVE route); CTA now routes to /connectors/browse

SPOTLIGHT FLOW FIXES (free-swap path was deadlocked end-to-end):
- BrowseConnectorsScreen: roster filter required a RATE CARD, silently excluding
  every free/swap Connector -> now filters on connected social handle
- RequestSpotlightModal: free/swap Connectors are now requestable ($0 swap);
  service pick required so the Connector sees what they get
- ConnectorRequestsScreen: free accepted requests no longer demand "Pay $0";
  Mark posted no longer gated on paid_at for free swaps; Counter hidden on $0
  asks (mathematically impossible); free-aware confirm/release copy
- CounterSpotlightModal: role-aware breakdown ("You earn" only for Connectors;
  providers see "You save / Connector earns")
- notify-spotlight: free-aware email copy (no "$0 rate card" / "funds released")
- NEW migration 20260605120000: expire sweep exempts free swaps (they are never
  "paid" by design — old sweep auto-expired every accepted free spotlight after
  24h) + rescues wrongly-expired rows

TESTING:
- New "Simulate 20 User Network.command": 20 users (10 Connectors — 5 free/swap
  + 5 paid — 10 Providers/services), dense friend graph, tier-1+2 invite chains,
  full free + paid spotlight lifecycles. 12/12 assertions PASS on live DB.
- qa.mjs: 41/41 invariants pass.
