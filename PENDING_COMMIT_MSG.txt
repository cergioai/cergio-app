fix(inbound): popping TrustStream + mutuals on the connector-request screen (SPEC-49g)

The /inbound request screen showed the key reputational stream in tiny text
("3 network on Cergio · 3 reco's made") and buried mutuals below the services
list. Now it uses the shared TrustStream so the differentiator pops, and mutuals
LEAD the stream.

- Replaced the small strength line with the shared <TrustStream> (big bold
  mutuals · network · recos numerals), full-width under the identity.
- Mutuals are now part of the headline stream (mutualCount), not buried — the
  named mutual faces stay below as the detail.
- recoKind="made" (a Connector requesting a free service is judged on recos made).

qa.mjs: #49g already covers the shared module. All 75 flows pass; screen
esbuild-clean.
