UX: leaf intensity on Home (engine-progress) + Splash + copy audit pass

LEAF INTENSITY EVERYWHERE THE LEAF MOVES

HomeScreen engine ticker leaf now scales intensity with planIdx —
the plant breathes faster as the scripted engine progresses through
its stages. Starts at 0.35 (soft) → ramps to 1.0 at the final stage.
Same shape as ResultsScreen's live-counts pulse, only the source is
scripted progress instead of DB rows.

SplashScreen leaf set to intensity=0.7 — a deliberate hold-still-
but-alive feel for the booting state. Loud enough to feel alive,
not so loud it competes with the wordmark.

HOME COPY AUDIT — no regressions found

Swept HomeScreen.jsx for stale terms:
  ✓ No "Cergio Cash" / "Cergio Coin"
  ✓ No hardcoded "$250" near reward-context words (REWARDS only)
  ✓ No "Expert" — "Connector" everywhere
  ✓ Headlines + sub-copy consistent with REWARD_COPY

qa #22 + #24 + #25 are the static enforcement; this commit is the
manual cross-check that came back clean.

VERIFIED  qa.mjs: 26/26 pass

Files:
  src/screens/HomeScreen.jsx
  src/screens/SplashScreen.jsx
