Notifications: fire on recommend + accept-with-time (SPEC-56)

Tarik 2026-06-18 — notifications/email audit. Two events wrote their row but
never fired a notification, so the recipient got no email/SMS:

- recommendService now fires notifyUser({ event: 'service_recommended',
  recipient: <service owner> }) — the notify-user edge fn already had that
  template; it just was never called. Best-effort, skips self, never blocks.
- acceptRequestWithTime now fires fireBookingNotify(bookingId, 'accepted') —
  this path creates the confirmed booking directly (no request_response row),
  so the requester was never told their request was accepted.

Audited the rest of the notify chain (request created, response, question,
spotlight created/countered/posted/confirmed, booking posted/confirmed/
complete/reschedule/dispute) — all already fire.

Also audited the search → results flow: honest empty states, no dead-end
buttons, offers render as provider cards routing to the PDP, waiting state has
live reply counts + inline cancel. No fixes needed.

FROZEN_SPEC SPEC-56 added; qa #56 added. All 59 flows green.
