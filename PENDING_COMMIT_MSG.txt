polish: PDP fixes, profile design pass, organic leaves, social feed, Stripe bypass

Bundle of fixes for Tarik's evening review session.

PDP (ServiceDetailScreen.jsx)
- "Book button hidden" — BottomNav was covering the fixed-bottom
  "Request {offering} ($N)" CTA. Added /service/ to HIDE_NAV_PREFIXES
  so the PDP renders without the nav (it's a focused booking flow).
- "Top image is cut off" — cover hero 120px → 280px so the photo
  dominates like the Jennifer Leighton mockup.
- "Reco'd by … not clickable" v3 — the row now is a single outer
  Link (not nested Links) to the lead recommender's /u/{id}, with
  hover/active background + a › chevron so it reads unmistakably as
  tappable. Bucket counts stay visually underlined.

Public profile (PublicProfileScreen.jsx)
- Design pass to match the mockup: avatar 64 → 72, name 26 → 28,
  section headers 20 → 22, tighter top spacing.
- {Name}'s Go-Tos cards rebuilt: dropped the redundant
  "Reco'd by {self}" inner row (we're already on their profile;
  every entry is implicitly by them). Comment bubble now shows the
  profile-owner's small avatar (matches the gray quote box on the
  mockup), Reco'd date pinned top-right, owner name linked to their
  /u/{id} too.

Logo (LeafLogo.jsx + index.css)
- v3 (8 identical ellipses) → v4 (7 unique <path> leaves). Each leaf
  has its own taper, width, midrib (center vein), tilt, opacity. Color
  palette spans 5 greens. Tarik: "more playful, leaves less symmetrical,
  like real leaves, different from each other".
- Keyframes swapped to cubic-bezier-overshoot (1.56 bounce) with a
  brief mid-bloom wobble — playful, not mechanical. 4 distinct
  keyframe shapes × 7 coprime durations × jittered delays = silhouette
  never repeats.

Tagline (SplashScreen.jsx + AuthScreen.jsx)
- "AI that negotiates and books services your friends actually trust."
  → "Hi, I'm Cergio. I'll negotiate and book services your friends
  actually trust." Matches the HomeScreen greeting voice. Sub-line
  "Book · Barter · Earn" preserved.

Activity feed (api.js listSocialFeed + ActivityScreen.jsx)
- Old feed only showed Connector go-to shares. New unified feed
  merges: friend AND Connector recommendations, new sign-ups
  ("X joined Cergio"), new service listings ("X listed a new service:
  Y"), confirmed Instagram/TikTok spotlights. All from real tables
  (recommendations / profiles / services / spotlight_requests).
  No fake feeds (feedback_no_fake_feeds preserved).
- ActivityScreen now renders four card kinds: reco (GoatShareCard),
  join (JoinedCard), listing (ListingCard), spotlight (SpotlightCard).
- Section heading switched to "What's happening on Cergio" /
  #cergiofeed.

Stripe-payouts bypass (RequestDetailScreen.jsx)
- Tarik: "disable the payment setup condition so we test... we can
  enable this regardless and then services are notified to set up to
  receive payment". Accept button is now always enabled. The Stripe
  reminder banner is non-blocking copy ("You can accept now — finish
  Stripe payouts setup so funds reach your bank"). On accept of a paid
  booking without payouts_enabled, a sticky toast reminds the provider
  to finish Stripe setup.

Verified: qa.mjs 41/41 · JSX syntax check on all touched files: green.
