fix(pdp): mutuals on "What people say" use friends-in-common, not raw network (SPEC-49g)

The PDP recommender rows checked raw network membership (getMyNetworkIds) for the
mutual signal, while the profile uses friends-in-common (getMutualConnections) —
so the profile showed "1 mutual friend in common" but the PDP showed nothing for
the same people. Unified to the friends-in-common signal.

- Fetch getMutualConnections for the displayed (top-3) recommenders →
  recommenderMutuals { count, names }.
- Each "What people say" row now shows the NAMED mutual line ("1 mutual friend in
  common — Jane", mutualNamesText) and the MutualBadge whenever count > 0.

All 75 flows pass; screen esbuild-clean.
