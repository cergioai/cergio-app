Below-4★ private review dispute: provider reply + escalate + admin hook (SPEC-47e)

- review_threads table + bookings.dispute_escalated_at (migration). getMyOpenDisputes /
  listReviewThread / addReviewReply (escalate stamps + pings support).
- DisputeCard: both parties see the rating + comment; reply back-and-forth; PROVIDER (rated
  low) can Escalate → flagged to Cergio/support (admin manual now; AI resolution phase 2).
  Private-review framing ("truthful reviews from trusted friends, not gamed reviews from strangers").
- JobsInbox: Overview "Ratings to resolve · N" action row + a Requests-tab section; badge counts them.
- FROZEN_SPEC SPEC-47e. The held spotlight goes live only at 4★+.
