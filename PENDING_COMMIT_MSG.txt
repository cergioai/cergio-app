Logo aligned on greeting line + "Forward to friends" rename

Two quick UX fixes from live testing:

(1) HOME LOGO VERTICALLY ALIGNED WITH GREETING
Tarik: "move up the logo to align with the 'hi what do you need'".
Was: flex items-start + pt-0.5 + size 44 — logo sat slightly above
text baseline and extended well below it, looking like a separate
element stacked on top of the greeting.
Now: flex items-center, no padding, size 36 — bud sits inline with
"Hi {name}, tell me what you need." line, shared centerline, reads
as one composed headline.

(2) "SEND TO FRIENDS" → "FORWARD TO FRIENDS"
Tarik: "forward to friends rather than send to friends". Forwarding
matches what the action actually does (re-share the prefilled link
via the native share sheet — there's no Cergio-side "send" happening,
the user just hands it off). Plus error toast updated to match.

Files:
  src/screens/HomeScreen.jsx
  src/screens/ResultsScreen.jsx

ALSO STAGED (on disk, not in cergio-app/):
  /Users/tariksansal/Downloads/Cergio Claude/Seed Miami Services.command
    Adds Maria/Sofia/Henry/Jackie service inventory in Miami Beach
    + recommendations from existing Alex/Connie/Sam friend network,
    so "house cleaner" / "driver" / "babysitter" searches return real
    cards with friend avatars + a free-for-GOATs Connector listing.
    Idempotent. Run AFTER Seed Money Flow Test Data.command.

QA: 41/41 pass.
