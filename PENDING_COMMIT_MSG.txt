outreach-send: add dry-run + test-send safety modes (SPEC-65)

?dry=1 returns counts (emailable / sms-phone-only / suppressed) with no send;
?test=<email> sends one sample to that address only (no DB writes) to verify
content + deliverability before any real blast. Protects domain reputation —
ramp gradually. qa 69 pass.
