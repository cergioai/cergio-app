/inbound: top headline + date/summary, mutual friends in tile, map below msg

Tarik 2026-06-14 finalize:
- Removed the "T" name from the header (back · ⚑ ↗ only).
- New top headline: "Free service request ⇄ Free spotlight to {N} followers"
  with the date chip beside it and a one-line summary (service · details).
- Mutual friends moved INTO the connector tile, next to the avatar, each name
  linked to their profile; explicit empty state "You have no mutual friends
  with {name} yet." (removed the separate friends block).
- Map moved BELOW the personalized message (just above the Accept button).
- Fixed the benefit line being cut off by the sticky bar: more bottom padding
  + a top border/shadow on the action bar.
- Summary line shows just the service (dropped the redundant category label).
