Reverse spotlight: dedicated detail screen (/spotlight/:id)

A frame-3-quality detail screen for an inbound spotlight request (provider →
Connector). Leads with the provider's SERVICE reputation — avatar · services
(each with reco's received) · bio · network on Cergio · IG (secondary) — then
the spotlight ask (free swap or paid, with the earn-after-fee math), the
provider's message, mutual friends, and Offer-the-spotlight / Set-your-price /
Decline actions (setSpotlightRequestStatus + counterSpotlightRequest).

- api: getSpotlightRequest(id) hydrates the provider's reputation.
- Route /spotlight/:id (+ added to HIDE_NAV_PREFIXES).
- ConnectorRequestsScreen inbound card now opens this detail screen.
