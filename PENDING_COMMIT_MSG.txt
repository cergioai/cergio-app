design(about): add "What we believe" — two foundational convictions

Tarik wording:
  "1-AI should be corrected by human recommendations (to correct the
   infected internet data) otherwise AI will just give you garbage
   more efficiently faster.
   2-We can create larger platforms by aligning user interests with
   shareholders ... which turns users into highly strategic partners
   earning income relative to their participation in the growth of
   the platform."

  Distilled into two crisp belief statements, dropped between the
  one-line mission ABOVE and the "Three ways Cergio creates value"
  streams BELOW — the WHY before the HOW.

New card on /about (mx-5 mt-5, white + border-line + rounded-[18px]):

  Eyebrow:  WHAT WE BELIEVE
  Belief 1: AI corrected by humans, not the other way around.
            "Internet data is polluted. Without endorsement from
             people you trust, AI just delivers garbage faster.
             Cergio routes every result through your real network —
             so the AI keeps getting corrected by humans."
  Belief 2: Platforms grow bigger when users own a piece.
            "Align user interests with shareholders and the network
             earns alongside the platform. Every Cergio user becomes
             a strategic partner — paid in income tied to platform
             growth, scaled to their participation."

Section style + spacing match the existing mission + streams cards
(same eyebrow tracking, 14/800 sub-heads, 13/500 body) so the page
reads as a single rhythm.

Verified: qa.mjs 41/41 · JSX syntax check on AboutScreen.jsx: green.
