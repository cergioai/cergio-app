# SPEC-237 — get the initial 100 creators, from BOTH sources, then pause

Founder, 2026-08-02: "activate the crawls just for the creator sources to get 100 of
each then pause alongside the rest to audit" · "set up CI subagents and dashboard and
share when they're live... ignore yelp as a source.. get the initial 100"

## Measured (not guessed)

1. Audit export 2026-08-02T17:38Z: **creators = 0** from both sources; ig_services has
   262 service rows and 0 creator rows.
2. `20260730190000_growth_schema_reference.sql` declares `is_business` inside
   `create table if not exists` but has NO `add column if not exists is_business`
   heal guard — the live table predates the reference, so the SPEC-202 42703 defect
   survived its own fix. `created_at` (written by creator-harvest) has the same gap.
3. `creator-harvest/index.ts` read NO control: no CRAWLS_SUSPENDED, no CRAWLS_ONLY,
   no CREATOR_TARGET. On its restored cron (SPEC-230, `17 */2 * * *`) it would run
   past 100 forever.
4. `growth-controls.json` CRAWLS_ONLY listed only `ig_services` — one of the two
   creator paths silently closed, the exact SPEC-230 failure shape.

## Change (one revertible PR)

- Heal guards `is_business` + `created_at` in the schema reference; verify probes
  writer columns (`apply-growth-schema.mjs`).
- creator-harvest: fail-closed CRAWLS_SUSPENDED / CRAWLS_ONLY check; self-stop at
  CREATOR_TARGET counted by PREFIX `se:web-harvest%`; refuses to crawl on an
  unreadable count; every skip/stop logged to agent_runs (idle ≠ unable).
- `growth-controls.json`: CRAWLS_ONLY = `ig_services,se:web-harvest`.
- Gate `#237` (mutation-tested), fleet.json creators brief + specs/creators.md updated,
  CLAUDE.md records the founder IG override verbatim.

## Live proof (after merge)

growth-setup fires on the controls change → schema heal + seed + kick; next audit
export must show creator rows > 0 with both discovered_via families, and agent_runs
must carry creator-harvest rows with target/suspend reasons.
